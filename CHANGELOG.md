# Changelog

All notable changes to this starter are documented here. Forks should read this file before every upstream merge — it is the single source of truth for what changed upstream and whether any of those changes affect fork-owned code or require migration work.

The format is based on [Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/). Versioning cadence: **every fork-visible breaking change cuts a new version**; non-breaking changes accumulate under `## [Unreleased]` until the next breaking change forces a new release. A "fork-visible breaking change" is anything that requires a fork to take action during a merge — removed files, renamed APIs, changed schemas, restructured directories, or modified contracts. Prior to 1.0.0, versions followed a `1.0.0-beta.N` naming convention while the fork-distribution model was being stabilized; all releases from 1.0.0 onward follow semantic versioning.

## [Unreleased]

### Added

- **Structured GitHub issue forms and a refreshed PR template ([#504](https://github.com/ResetShop/angular-nx-standalone-starter/issues/504)).** Added six GitHub issue-forms templates under `.github/ISSUE_TEMPLATE/` — `bug-report.yml` (`🦟 bug`), `feature.yml` (`🏎️ enhancement` + `✍️ draft`), `tooling.yml` (`🛠️ tooling` + `✍️ draft`), `development-process.yml` (`💼 management` + `✍️ draft`), `ui-ux.yml` (`🎨 ui-ux` + `✍️ draft`), and `docs.yml` (`📝 documentation` + `✍️ draft`) — plus three classic Markdown templates for maintainer-authored tracking issues: `release.md` (`💼 management`), `epic.md` (`📦 epic`), and `initiative.md` (`🧩 initiative`), each pre-filling a sections-and-task-list body so constituent issues/epics render as GitHub-tracked sub-issues. Each form uses the GitHub issue-forms schema (`name`, `description`, `labels`, `body` with typed `checkboxes`/`dropdown`/`textarea` elements) and applies default labels from the repo's existing set; `bug_report.yml` and `feature.yml` carry an optional area/layer dropdown (`Frontend (Angular / NgRx)`, `Backend API (Hono / Drizzle / Zod)`, `Nx Generators`, `Tooling / CI`, `Documentation`, `Other`) to aid triage. The templates are adapted (translated to English and re-domained for this Nx + Angular + Hono starter) from [cuentoneta/cuentoneta](https://github.com/cuentoneta/cuentoneta)'s issue templates. Two repo labels are introduced — **`✍️ draft`** (English of cuentoneta's `✍️ bosquejo`, marking freshly-proposed issues pending refinement) and **`🧩 initiative`** (a planning tier above `📦 epic`) — and the unused literature-specific **`📜 content`** label is removed. The existing `.github/ISSUE_TEMPLATE/config.yml` (security advisory + contribution-policy links, `blank_issues_enabled: true`) is preserved unchanged, and `.github/pull_request_template.md` gains a `Closes #<issue-number>` linking reminder in its Summary section. Documentation/config only — no runtime, API, or schema change. **Fork migration:** forks inherit the templates and PR template automatically; fork maintainers should create the `✍️ draft` and `🧩 initiative` labels in their own repo (or retune the templates' `labels:` arrays to match their own label names) since GitHub silently drops default labels that don't exist in the target repo. ([#504](https://github.com/ResetShop/angular-nx-standalone-starter/issues/504))
- **CI falls back to Nx's local cache when Nx Cloud credits are exhausted ([#529](https://github.com/ResetShop/angular-nx-standalone-starter/issues/529)).** Every CI job runs Nx tasks with `NX_CLOUD_ACCESS_TOKEN` set (inherited from the workflow-level `env`), so when the Nx Cloud free credits run out the workspace is disabled and Nx aborts **every** task with a non-zero exit code — even though the underlying task (tests, lint, build) would pass — blocking every PR until credits reset. A new wrapper `.github/scripts/nx-run.sh` runs `npx nx <task>` and, **only** when the failure output matches a "workspace is disabled / Nx Cloud limit/quota/exceed" pattern, retries once with `NX_NO_CLOUD=true` so the task runs against Nx's local cache instead of failing the pipeline; any other failure (a broken test, red lint, etc.) is propagated unchanged. All eight Nx steps in `.github/workflows/ci.yml` are routed through the wrapper (`test`, `typecheck`, `lint`, `stylelint`, `test-integration`, `e2e`, `build`, `storybook`) — `test-integration`'s underlying Nx command is inlined through the wrapper rather than going via `npm run test:integration`, matching how `e2e`/`build`/`stylelint` already inline their Nx commands. A new root `.gitattributes` pins `*.sh` to `text eol=lf` so the script is not CRLF-corrupted when edited on a Windows checkout (CI runs it with bash on Linux). Ported from [cuentoneta/cuentoneta#1628](https://github.com/cuentoneta/cuentoneta/pull/1628); unlike that repo (which rides a pre-existing per-job `.nx/cache` `actions/cache`), this repo caches the whole repo keyed by `github.sha`, so the fallback runs cold locally — it still unblocks CI, just without an accelerated local cache. CI-infrastructure only; no runtime, API, or schema change. **Fork migration:** none required; forks inherit the resilience automatically. ([#529](https://github.com/ResetShop/angular-nx-standalone-starter/issues/529))
- **`railway.json` deploy config (build/start/healthcheck as code) ([#514](https://github.com/ResetShop/angular-nx-standalone-starter/issues/514)).** Added a root `railway.json` that codifies the Railway deployment instead of leaving it only in the service UI: `build.buildCommand` (`npm run build` → `dist/reference-app/server/server.mjs`), `deploy.startCommand` (`npm run serve:ssr`), a `deploy.healthcheckPath` of `/api/health/v1` (the app's public health endpoint), and an `ON_FAILURE` restart policy with a bounded retry count. The healthcheck gates a deploy on a reachable database, so a misconfigured deploy (e.g. a wrong `PG_CONNECTION_STRING`) can no longer replace a healthy one, and the bounded retries cap the restart loop the startup `verifyHealth` would otherwise produce. Makes deploys reproducible across prod and PR-preview environments and inherited by forks. **Note:** Railway config-as-code takes precedence over the service UI — the committed `buildCommand`/`startCommand` match the current settings, and `healthcheckPath` is additive. ([#514](https://github.com/ResetShop/angular-nx-standalone-starter/issues/514))
- **`CODEOWNERS` review routing ([#488](https://github.com/ResetShop/angular-nx-standalone-starter/issues/488)).** Added `.github/CODEOWNERS` routing all paths to the ResetShop maintainers, so a review from any one of them satisfies a "Require review from Code Owners" branch-protection rule (enabling that rule is a maintainer GitHub-settings step). This is the final in-repo item of the public-readiness epic; the remaining epic items are GitHub-settings toggles. **Fork migration:** forks should replace the owner list with their own maintainers, or delete the file. ([#488](https://github.com/ResetShop/angular-nx-standalone-starter/issues/488))
- **Community-health files and an org-only-PR enforcement workflow ([#493](https://github.com/ResetShop/angular-nx-standalone-starter/issues/493)).** Added `CONTRIBUTING.md`, `SECURITY.md`, and `CODE_OF_CONDUCT.md` (Contributor Covenant v2.1), `.github/ISSUE_TEMPLATE/config.yml` (issues stay open to everyone; routes vulnerabilities to GitHub Private Vulnerability Reporting and surfaces the policy), and a `.github/workflows/close-external-prs.yml` workflow (`pull_request_target: [opened]`, `permissions: pull-requests: write`) that checks ResetShop org membership via an `ORG_READ_TOKEN` PAT (`read:org` scope) and auto-closes PRs from non-members with a policy comment. **Policy: issues are welcome from anyone; pull requests are accepted from ResetShop members only.** The workflow **fails safe** — any membership-check error (absent/expired `ORG_READ_TOKEN`, network, or unexpected status) leaves the PR open rather than wrongly closing a real member's PR — and contains **no checkout and no PR-branch execution** (documented by an in-file `pull_request_target` safety comment). A `> [!NOTE]` policy banner is added near the top of `README.md`, and `CODE_OF_CONDUCT.md` is added to `.prettierignore` (verbatim Covenant text). Structured issue/PR templates are tracked separately in [#504](https://github.com/ResetShop/angular-nx-standalone-starter/issues/504). **Fork migration:** the workflow is hardcoded to the `ResetShop` org and gated by `if: github.repository == 'ResetShop/angular-nx-standalone-starter'`, so it is inert on forks; forks wanting their own enforcement must change the `if:` condition and the `org` constant, or delete the workflow. Enabling GitHub Private Vulnerability Reporting and provisioning the `ORG_READ_TOKEN` secret are manual GitHub-settings steps (see the PR checklist). ([#493](https://github.com/ResetShop/angular-nx-standalone-starter/issues/493))
- **`LICENSE.md` and `NOTICE.md` files — Apache-2.0 adoption ([#490](https://github.com/ResetShop/angular-nx-standalone-starter/issues/490)).** Root `LICENSE.md` contains the verbatim Apache License 2.0 text (authored as a Markdown file). Root `NOTICE.md` carries the copyright attribution (`Copyright 2026 ResetShop`) required by Apache-2.0 section 4(d) plus a cross-reference to the in-tree third-party attributions (EFF/Dadoware wordlists) so a redistributor propagating only the NOTICE file is pointed at them. Both files are added to `.prettierignore` so the pre-commit Prettier hook never reflows the canonical license text. A new `## License` section in `README.md` makes the license discoverable from the repo front page. No per-file source headers are added — `LICENSE.md`, `NOTICE.md`, and SPDX `license` fields in every `package.json` together provide sufficient, machine-readable Apache-2.0 attribution. The repo previously shipped no license file, so the default was _all rights reserved_; this is the first change of the [#488](https://github.com/ResetShop/angular-nx-standalone-starter/issues/488) public-readiness epic (B1). ([#490](https://github.com/ResetShop/angular-nx-standalone-starter/issues/490))

### Changed

- **Repo-wide CI guard scripts promoted to a single Nx `check` target; `ci`/`ci:verify` no longer chain guards with `&&` ([#527](https://github.com/ResetShop/angular-nx-standalone-starter/issues/527)).** The two repo-wide guard scripts (`scripts/check-no-env-files.mjs`, `scripts/check-no-issue-refs-in-comments.mjs`) and `prettier --check` are now modelled as one `check` Nx target on the root `@app/source` project, declared in the root `package.json` `"nx"."targets"` block via `nx:run-commands` (`parallel: false` for `&&`-style short-circuit, `cache: false`). `npm run ci` and `npm run ci:verify` are simplified from a leading `&&`-chain of `node scripts/…` calls into two `nx run-many` invocations each — Batch 1 is now `check stylelint lint typecheck`, Batch 2 is unchanged (`test test-integration build build-storybook`). Adding a future repo-wide guard now means appending one line to the `check` target's `commands` array; the `ci`/`ci:verify` scripts never grow again. `cache: false` is intentional — these guards inspect working-tree state (e.g. the _presence_ of a `.env*` file), which Nx's content-hash cache cannot capture, so they must always run; `prettier --check` keeps its own `--cache` for per-file speed, and the genuinely cacheable targets (`stylelint`/`lint`/`typecheck`/`test`/`build`/`build-storybook`) are untouched and still ride the Nx cache under `ci:verify`. The `.husky/pre-commit` hook keeps its direct `node scripts/…` invocations for zero-overhead commit-time feedback (the Nx target is the CI entry point; the hook is the commit-time one). A `check` job is also added to `.github/workflows/ci.yml` (`npx nx run @app/source:check`, run with `NX_NO_CLOUD=true` so the guards never depend on Nx Cloud availability or plan quota — `check` is `cache: false` and gains nothing from the cloud, and a disabled/over-quota cloud org otherwise makes `nx` hard-exit before the target runs), so the guards now enforce at all three layers — commit hook, local `npm run ci`/`ci:verify`, and remote CI — closing the gap where a `--no-verify` push previously skipped them remotely (the guards had never run in the remote pipeline, which executes per-target jobs rather than `npm run ci`). Tooling-config-only change; no application runtime behavior change. **Fork migration:** none required; forks that reference the old `ci`/`ci:verify` script bodies or add their own guard scripts should add new guards to the `check` target's `commands` array rather than re-introducing an `&&` chain, and inherit the remote `check` job automatically. ([#527](https://github.com/ResetShop/angular-nx-standalone-starter/issues/527))
- **Comment-quality policy: no issue-number or before/after references in code comments, plus a codebase sweep and an enforcement guard ([#499](https://github.com/ResetShop/angular-nx-standalone-starter/issues/499)).** Code comments must describe present-state rationale only — what the code does and why it must be this way now — never the history of how it got there. The policy is recorded in `CLAUDE.md` (a new "Code Comment Quality" subsection plus a Hard Constraints table row) and in `.claude/references/coding-agent-policies.md` (new Section 6), banning GitHub issue/PR number tokens (`#<n>`), before/after-issue framing (`Since #497…`, `Pre-#331 this was…`), and "formerly X" / "moved here from" framing from `.ts` / `.spec.ts` / `.stories.ts` comments. The policy also states positively that **PRs and issues are the canonical home for change-narrative documentation** — the why-it-changed, the before/after story, and the issue/PR cross-references live in the PR description and the issue thread (a code comment documents the present invariant; the PR and issue document the change that produced it). Swept 15 comment sites across 13 source files, rewriting each as a present-state description (e.g. `// regression guard for #471` → a plain-English statement of the singleton-bridge invariant). Added `scripts/check-no-issue-refs-in-comments.mjs` — modelled on `check-no-env-files.mjs`, scanning every `.ts` file under `apps/` and `packages/` for a `#[1-9]\d{2,}` token in a `//` or block-comment line — wired into the `.husky/pre-commit` hook and both the `ci` and `ci:verify` npm scripts. `CHANGELOG.md`, `docs/`, `README.md`, and `.claude/` are exempt by path (hyperlinked issue references there are durable cross-references). No runtime behavior change. **Fork migration:** none required, but forks carrying issue-number references in `.ts` comments will fail the new pre-commit/CI guard after merging — rewrite those comments as present-state rationale (or, for genuinely durable cross-references, move them to `CHANGELOG.md` or a `docs/` file). ([#499](https://github.com/ResetShop/angular-nx-standalone-starter/issues/499))
- **`@reference "#tailwind-theme"` is now auto-injected at build time and removed from component source ([#523](https://github.com/ResetShop/angular-nx-standalone-starter/issues/523)).** Tailwind v4 requires each isolated component stylesheet that uses `@apply` to declare `@reference` so the directive resolves against the theme; previously every such stylesheet carried the line in source, and the `ui-component` generator emitted it into every new component. A new root PostCSS plugin `postcss-inject-tailwind-reference.js` (registered first in `.postcssrc.json`, before `@tailwindcss/postcss`) prepends `@reference "#tailwind-theme"` at build time to every stylesheet that uses `@apply` — covering all three pipelines that read `.postcssrc.json` (the Angular esbuild builder, Storybook's webpack `postcss-loader`, and Vite/Vitest). Injection is gated on `@apply` because that is the only thing the reference resolves: global/theme-emitting stylesheets (e.g. `styles.css`, which `@import`s the theme to **emit** it) are deliberately skipped, since referencing the theme there double-references it and suppresses its emitted `@theme` tokens (e.g. the `--font-*` variables). The plugin is idempotent (skips a stylesheet that already declares `@reference`) and excludes theme-entry files (`tailwind.config.css`, `accessibility.css`), which must keep `@reference "tailwindcss"` to avoid a circular self-reference. The plugin and `.postcssrc.json` are registered as nx `sharedGlobals` so build/test/storybook caches invalidate when they change. The directive was removed from all 8 component source stylesheets (`dashboard`, `nav-item`, `sidebar`, `button`, `form-field` inline `styles:`, and `toast-notification.css`/`select.css`/`combobox.css` sidecars), and the `ui-component` generator (`EXTERNAL_STYLE_BODY`, the inline template, the spec, and `generators.md`) no longer emits it. Payload is unaffected — `@reference` produces no output CSS. **Migration:** forks that author component stylesheets no longer write `@reference "#tailwind-theme"` (it is injected automatically); a new file `@import`ed into `tailwind.config.css` that uses `@apply` must be added to the plugin's theme-entry exclusion list (a miss surfaces as a loud Tailwind circular-reference build error). ([#523](https://github.com/ResetShop/angular-nx-standalone-starter/issues/523))
- **`no-process-env` ESLint allowlist tightened to least-privilege; integration tests routed through the env schemas ([#501](https://github.com/ResetShop/angular-nx-standalone-starter/issues/501)).** The `no-process-env-allowlist` block in `eslint.config.mjs` previously lifted the `process.env` restriction for three directory wildcards (`apps/**/src/api/config/**/*.ts`, `apps/**/src/api/integration/**/*.ts`, `apps/**/src/db/**/*.ts`), silently permitting raw `process.env` access in every file under those trees. It is now an explicit per-file list (no `/**/` directory wildcards — `**` remains only as a multi-app prefix): `env-utils.ts`, `env-utils.spec.ts`, `config/runtime.ts`, `config/runtime.spec.ts`, `test-setup.ts`, `integration/setup/global-setup.ts`, `integration/setup/env-helpers.ts`, and `db/seed.ts`. To get there: a new `apps/reference-app/src/api/config/runtime.ts` exports `isInteractive()` (reads `CI` + `stdin.isTTY`; `seed.ts` now uses it); `integration/setup/env-helpers.ts`'s `configureEnvVars` now seeds the typed caches via `seedDbEnv`/`seedTokenEnv`/`seedEmailEnv`/`seedPasswordEnv`/`seedCronEnv` (the same Zod parse the proxies run) instead of writing `process.env`; `db-helpers.ts`, `auth-helpers.ts`, and `login.integration.spec.ts` now read `appEnv.INTEGRATION_TEST_ADMIN_PASSWORD` / `dbEnv.PG_TEST_CONNECTION_STRING`; and the generic proxy `process.exit(1)` FATAL test moved from `db.env.spec.ts` into a new `env-utils.spec.ts`. `global-setup.ts` keeps its raw `process.env['PG_TEST_CONNECTION_STRING']` write (worker child processes inherit it via the OS environment — in-memory seed caches do not cross process boundaries), and `env-helpers.ts` keeps `loadEnvFile` as the single sanctioned integration `.env` delivery boundary (its `getTestConnectionString` reads `process.env` raw because it runs before the caches are seeded). **Migration:** forks that added direct `process.env` reads anywhere under `src/api/config`, `src/api/integration`, or `src/db` other than the eight listed files will now fail `npm run lint` on the next merge — route the value through the appropriate `<domain>Env` proxy (or `seed<Domain>Env` in tests), or add a justified explicit entry to the allowlist. ([#501](https://github.com/ResetShop/angular-nx-standalone-starter/issues/501))
- **Dashboard header chrome tokenized — hardcoded grays replaced with semantic theme tokens ([#520](https://github.com/ResetShop/angular-nx-standalone-starter/issues/520)).** The dashboard header chrome used hardcoded Tailwind palette colors and `dark:`-only one-offs instead of the design system's semantic theme tokens. `apps/reference-app/src/app/components/header/header.ts`'s host binding carried a dangling `dark:text-gray-50` (a dark-only text color with no light-mode counterpart), and `apps/reference-app/src/app/pages/dashboard/dashboard.ts`'s `<header appHeader>` element used `border-gray-200` / `dark:border-white/10` / `dark:bg-black/95`. These now use `text-foreground`, `border-border`, and `bg-background` — the oklch-backed tokens defined in `tailwind.config.css` that adapt to light/dark automatically, matching the rest of the app (`breadcrumb`, `landing-header`). The swap is visually exact in both modes: `--foreground` dark ≈ `gray-50`, `--border` dark = `white/10`, and `--background` dark ≈ `black/95` (chosen over `bg-card`, whose dark value `20.5%` is lighter than the prior `black/95` ≈ `14.5%`). The header-chrome wrapper elements in `header.stories.ts` were repointed to the same tokens so Storybook renders the header as the app now does; the surrounding sidebar/main/label simulation chrome in those stories is left untouched (out of scope, per the issue). Surfaced by the [#489](https://github.com/ResetShop/angular-nx-standalone-starter/issues/489) host-migration code review, which carried the `dark:text-gray-50` over verbatim. No behavior, API, or schema change, and no fork migration required. ([#520](https://github.com/ResetShop/angular-nx-standalone-starter/issues/520))
- **Host-level `:host { @apply … }` styles migrated to the `host: { class }` decorator binding, and the `@reference` target standardized ([#489](https://github.com/ResetShop/angular-nx-standalone-starter/issues/489)).** Components whose host styling was unconditional, host-only, and purely utility-based now declare those utilities via `host: { class: '…' }` instead of an encapsulated `:host { @apply … }` block, deleting the now-empty `styles` blocks and their orphaned `@reference` lines: `brand`, `header`, `immersive-panel`, the four auth pages (`login`, `change-password`, `reset-password`, `reset-password-confirm`), and the host line of `loading-spinner` (its `.spinner-ring`/`@keyframes` rules remain). `nav-item` is partially migrated — its unconditional `cursor-pointer text-sm` moves to `host: { class }` while the conditional `:host(.collapsed)` and descendant rules stay. **Behavioral note:** `:host { @apply }` emits view-encapsulated CSS (`[_nghost-…]` specificity); `host: { class }` emits global utility classes at base specificity — equivalent for these layout utilities since nothing overrides them on the host. The `@reference` target string is standardized to `"#tailwind-theme"` for all isolated consumer stylesheets (`dashboard`, `button`, `toast-notification.css`, plus `nav-item`), with one structurally-required exception: `packages/ui/src/theme/accessibility.css` is `@import`ed into the theme entry (`tailwind.config.css`) and so must keep `@reference "tailwindcss"` (referencing `#tailwind-theme` from inside the theme it composes is circular). A `@reference "#tailwind-theme"` must also sit at the top of its stylesheet, never nested in a rule, because the theme entry carries `@source` (which cannot be nested) — `dashboard`'s `@reference` was hoisted out of its `:host` block accordingly. The non-host cases (`sidebar`, `button`/`form-field` projected children, the standalone `select.css`/`combobox.css`/`toast-notification.css`) intentionally keep `@apply`/`@reference` — `host: { class }` cannot express projected-child, conditional, or non-utility styling. Full project-wide `@reference` elimination (build-time auto-injection or a raw-CSS rewrite) was evaluated and deferred to [#523](https://github.com/ResetShop/angular-nx-standalone-starter/issues/523), which carries the findings; the migration's measured payload delta was +239 B / +0.5% (payload was never the motivation). No fork migration required — the rendered output is unchanged. ([#489](https://github.com/ResetShop/angular-nx-standalone-starter/issues/489))
- **Standalone DB scripts consolidated onto the canonical Drizzle connector ([#502](https://github.com/ResetShop/angular-nx-standalone-starter/issues/502)).** `apps/reference-app/src/db/seed.ts` and `sync-permissions.ts` previously each built their own `new Pool()` + `drizzle(pool)` instance (and `seed.ts` derived an ad-hoc `type Tx` from it). They now use `createDrizzlePgConnector()` and the exported `DrizzleTransaction` type from `apps/reference-app/src/api/helpers/drizzle-postgres-connector.ts` — the same connector the production API and all repositories use. This removes the duplicated connection wiring and the local transaction type, and the scripts gain the connector's idle-client `error` listener (a connection drop/restart mid-run no longer risks an unhandled-error process crash). The connector is created inside each script's entry function (function scope, not module scope), and both scripts now close their connection pool via `db.$client.end()` before exiting (the `process.exit` calls moved to the entry point so the `finally` runs). Runtime behavior is otherwise unchanged — both scripts use the same core `select`/`insert`/`transaction` calls, for which the schema-typed connector is a superset. The connector's `@schema/*` / `@config/*` / `@resetshop/util` path-alias imports resolve under the scripts' `tsx --tsconfig apps/reference-app/tsconfig.json` invocation (verified). `e2e/setup/db-seed.ts` intentionally stays on direct `bcrypt`/`pg` wiring (Playwright's loader resolves these aliases unreliably). No migration needed for forks. ([#502](https://github.com/ResetShop/angular-nx-standalone-starter/issues/502))
- **Pre-public repo hygiene — personal email removed from source, committed `error.log` deleted, root `package.json` metadata added ([#494](https://github.com/ResetShop/angular-nx-standalone-starter/issues/494)).** Removed the `@author Ramiro Olivencia <…>` JSDoc line from `apps/reference-app/src/api/utils/clarity.utils.ts` so the maintainer's personal email is no longer GitHub-code-search-indexed. `git rm`'d the stray committed `error.log` artifact and added a catch-all `*.log` rule to `.gitignore`. Added standard metadata to the root `package.json` — `description`, `repository`, `homepage`, `bugs`, `author` (`"ResetShop"`, deliberately not a personal email), and `keywords`; the root `name` stays `@app/source` (a `private: true` root kept deliberately generic for forks). Added CI/license/Node badges and a one-line "what is this" intro to `README.md`. The CI `${{ vars.* }}` → `${{ secrets.* }}` move, Secret Scanning/Push Protection, Dependabot, and the `nxCloudId` decision are maintainer GitHub-settings follow-ups documented in the PR (not changed here). ([#494](https://github.com/ResetShop/angular-nx-standalone-starter/issues/494))
- **Admin seed credentials are now operator-supplied, not hardcoded ([#491](https://github.com/ResetShop/angular-nx-standalone-starter/issues/491)).** `apps/reference-app/src/db/seed.ts` previously created an admin with a known email (`admin@sistema.com`) and a committed bcrypt hash — offline-crackable once public, and inherited by every deployer who ran `npm run drizzle:seed` unchanged. Credentials are now resolved in precedence order: (1) `SEED_ADMIN_EMAIL` + `SEED_ADMIN_PASSWORD` env vars (CI/Docker/prod-safe, non-interactive); (2) an interactive TTY prompt with masked password input and re-prompt on invalid entry; (3) fail-fast with a clear, actionable error. `SEED_ADMIN_FIRST_NAME` / `SEED_ADMIN_LAST_NAME` are optional (default `Admin` / `User`) — all four are declared in the `app` env sub-schema. The password is hashed at runtime via the app's `createPasswordHasher()` (cost from `passwordEnv.BCRYPT_COST`). The resolution logic lives in a new, fully unit-tested `seed-admin-credentials.ts`. The seed stays idempotent — an existing admin row is left untouched. **Migration:** set `SEED_ADMIN_EMAIL` and `SEED_ADMIN_PASSWORD` (12–128 chars) before running `npm run drizzle:seed` in CI/non-interactive contexts, or run it in a terminal to be prompted; existing fork databases are unaffected (the seed never overwrites existing rows). ([#491](https://github.com/ResetShop/angular-nx-standalone-starter/issues/491))
- **`postinstall` no longer runs database migrations or the seed ([#491](https://github.com/ResetShop/angular-nx-standalone-starter/issues/491)).** `npm install` / `npm ci` previously ran `npm run drizzle:push-migrations && npm run drizzle:seed` automatically, which failed the install on any machine without `PG_CONNECTION_STRING` set and would hang on the seed's interactive prompt in piped contexts. The hook now runs `node scripts/setup-skills.mjs` only; the database steps are explicit. **Migration:** any CI/CD pipeline, Dockerfile, or fork that relied on `npm install` to apply migrations must add an explicit `npm run drizzle:push-migrations && npm run drizzle:seed` step (after configuring `PG_CONNECTION_STRING` and the `SEED_ADMIN_*` vars) following the install step. ([#491](https://github.com/ResetShop/angular-nx-standalone-starter/issues/491))
- **`auth.env.ts` decomposed into `token` / `password` / `security` sub-schemas; `CRON_SECRET` folded into `cron` ([#497](https://github.com/ResetShop/angular-nx-standalone-starter/issues/497)).** The monolithic `auth` env sub-schema bundled four unrelated concerns behind its two required PASETO fields, so reading any optional field (e.g. `BCRYPT_COST`) in a context without `PASETO_SECRET_KEY` / `PASETO_ISSUER` set — a seed-only run, the SSR prerender worker — triggered a FATAL `process.exit(1)`. `auth.env.ts` is now deleted and replaced by `token.env.ts` (`PASETO_*`, `COOKIE_SECURE`), `password.env.ts` (`BCRYPT_COST`), and `security.env.ts` (`AUTH_MAX_FAILED_ATTEMPTS`, `AUTH_LOCKOUT_DURATION`, `AUTH_CHANGE_PASSWORD_RATE_LIMIT_WINDOW`/`_MAX`); `CRON_SECRET` moved into the existing `cron.env.ts`. The env contract now spans eight sub-schemas. Consumers were repointed (`password-hasher` → `passwordEnv`, `paseto.config` → `tokenEnv`, `rate-limit.middleware` → `securityEnv`, `cron-jobs` → `cronEnv`); `createAuthConfig` now aggregates the token/security/cron proxies via three defaulted source parameters. **The runtime env-var names are unchanged (`PASETO_*`, `BCRYPT_COST`, `AUTH_*`, `CRON_SECRET`) — deployers take no action.** **Fork migration:** forks that import `authEnv`, `parseAuthEnv`, `seedAuthEnv`, `resetAuthEnv`, or `type AuthEnv` from `@config/auth.env` must repoint — `PASETO_*`/`COOKIE_SECURE` → `@config/token.env` (`tokenEnv` / `TokenEnv` / `seedTokenEnv` / …), `BCRYPT_COST` → `@config/password.env`, the `AUTH_*` lockout/rate-limit fields → `@config/security.env`, and `CRON_SECRET` → `@config/cron.env`; `@config/auth.env` no longer exists. ([#497](https://github.com/ResetShop/angular-nx-standalone-starter/issues/497))
- **`package.json` `license` fields set to `"Apache-2.0"` across root and all five `packages/*` ([#490](https://github.com/ResetShop/angular-nx-standalone-starter/issues/490)).** Root `package.json` corrects the previous invalid, non-SPDX `"CC-BY-SA 4.0"` value (a content license CC recommends against for code). `packages/angular-core`, `packages/generators`, `packages/hono-core`, `packages/ui`, and `packages/util` gain the `license` field for the first time. **Fork migration:** forks publishing their own packages should set their `package.json` `license` fields to whichever license applies to their fork-owned code; forks redistributing the unmodified starter inherit the Apache-2.0 section 4(d) obligation to include the `NOTICE.md` attribution. ([#490](https://github.com/ResetShop/angular-nx-standalone-starter/issues/490))
- **CEUT FRSF branding replaces the generic starter identity in `apps/ceut-frsf` ([#1](https://github.com/ResetShop/ceut-frsf/issues/1)).** Fork-local rebrand of the `ceut-frsf` app — no upstream-owned file (`apps/reference-app`, `packages/*`, workspace-root `public/`) touched. The sidebar `Brand` component (`apps/ceut-frsf/src/app/components/brand/brand.ts`) now renders the CEUT FRSF logo instead of the `featherRefreshCw` icon + hardcoded "Reset Starter Repo" text: the full horizontal logo (`public/logo/ceut-logo.svg`) when expanded and a compact square icon (`public/logo/ceut-icon.png`, the CEUT "raised hands" mark) when the sidebar is collapsed, with `alt="CEUT FRSF"` supplying the link's accessible name in both states; its home link is now the absolute `['/dashboard']` (resolving the prior stale relative TODO). The favicon is the authentic CEUT `.ico`, and — critically — `apps/ceut-frsf`'s build asset input was switched from the workspace-shared `public/` to an app-local `apps/ceut-frsf/public/` (favicon, logo, and duplicated Geist font files) so CEUT branding never leaks into `apps/reference-app`, which keeps serving the shared root `public/` unchanged. `LANDING.BRAND_NAME` is `CEUT FRSF` and `HERO_HEADING`/`HERO_SUBHEADING` carry on-brand placeholder copy in both locales (`es.ts`/`en.ts`/`translation.mock.ts`); final hero copy and the full visual identity (colors, typography, home redesign) are deferred to a separate design pass. The landing page's three-card starter-feature grid is removed for a minimal hero-only home — the `LANDING.FEATURES.*` keys remain defined but unrendered because the shared `TranslationSchema` in `packages/angular-core` requires them and editing that upstream schema is out of scope for a fork-local rebrand. The auth pages' placeholder logo `alt="Your Company"` is corrected to `alt="CEUT FRSF"`, and the OpenAPI spec title is now `"CEUT FRSF API"`. Fork-local change; not inherited by other forks and not upstreamed. ([#1](https://github.com/ResetShop/ceut-frsf/issues/1))

### Fixed

- **Health view panels now render correctly in dark mode ([#513](https://github.com/ResetShop/angular-nx-standalone-starter/issues/513)).** The Application Health Checker page (`apps/reference-app/src/app/pages/dashboard/pages/health/health.ts`) hardcoded light-only Tailwind palette classes in its template — `border-gray-200` on the outer box and `bg-gray-100` on the overall-status and database-checks panels — so the two inner panels stayed near-white and the border invisible against the dark theme. They now use the project's semantic theme tokens `border-border` and `bg-muted` (the same tokens every other card uses), which carry distinct light/dark values, so both panels adapt to the active theme. The stray `border-1` was normalized to the canonical `border` shorthand at the same time. A new `health.stories.ts` (Loading / Healthy / Unhealthy / ApiError stories, HTTP mocked via `withInterceptors`) is added as the visual-regression guard — toggling the Storybook background to dark exercises the panels against the dark theme. No API, schema, or behavior change. ([#513](https://github.com/ResetShop/angular-nx-standalone-starter/issues/513))
- **Integration-test teardown hardened — no orphaned worker processes, embedded-Postgres temp dirs cleaned up, and CI peak memory bounded ([#517](https://github.com/ResetShop/angular-nx-standalone-starter/issues/517)).** Five compounding teardown issues were resolved:
  1. **Orphaned, memory-retaining `node` worker.** The app's Drizzle/node-postgres pool (the Awilix `db` singleton) was never closed after the suite, leaving idle TCP sockets that kept the Vitest worker alive — intermittently leaving a ~600 MB process behind. A new `DependencyContainer.teardownDb()` method (`apps/reference-app/src/api/container/container.ts`) closes `db.$client` and is invoked from the `afterAll` in `apps/reference-app/src/api/integration/setup/integration-setup.ts` (inside a `try/finally`, so it runs even if the test-helper pool close throws). Under Vitest's default per-file isolation each test file owns its container and pool, so this closes that file's own pool — it does **not** reintroduce the mid-run "empty results" hazard the old comment guarded against (which only applies to a pool shared across files). `teardownDb()` is a lifecycle method on the concrete container only, deliberately not added to the `Container` interface (mirroring `verify`/`use`/`restore`), so `InMemoryContainer` is unchanged.
  2. **Leaked embedded-Postgres data dirs on Windows.** `stopEmbeddedPostgres` (`apps/reference-app/src/api/integration/setup/embedded-pg-test-db.ts`) now wraps `pg.stop()` and runs its own `rmSync(dir, { recursive, force, maxRetries: 3, retryDelay: 200 })`, absorbing the Windows file-lock race that the library's own `fs.rm` loses.
  3. **Stale-dir sweep.** `startEmbeddedPostgres` now sweeps `os.tmpdir()` for `rs-integration-pg-*` directories lacking a `postmaster.pid` (interrupted runs) and removes them before starting a fresh cluster.
  4. **Always-stop teardown.** `teardown()` in `apps/reference-app/src/api/integration/setup/global-setup.ts` is restructured with `try/finally` so the embedded cluster is stopped even if table truncation throws.
  5. **Bounded memory.** `vitest.integration.config.ts` gains `teardownTimeout: 30_000`; `vitest.config.ts` pins `pool: 'forks'` with `maxWorkers: 2` (the Vitest 4 replacement for the removed `poolOptions.forks.maxForks`, overridable via the `VITEST_MAX_WORKERS` env var — see `docs/environment-variables.md`); and the heavy-task batch of both the `ci` and `ci:verify` scripts gains `--maxParallel=3`. The `.nx` daemon log is already gitignored, so no change was needed there.

  CI (Linux, `postgres:17` service container with `PG_TEST_CONNECTION_STRING` set) skips the embedded-Postgres paths entirely and is behaviorally unchanged. No fork migration required — these are test-infrastructure and CI hardening changes; forks inherit a cleaner teardown automatically. ([#517](https://github.com/ResetShop/angular-nx-standalone-starter/issues/517))

- **Stale `file:line` citations and a broken link cleaned up in `README.md` ([#492](https://github.com/ResetShop/angular-nx-standalone-starter/issues/492)).** Full accuracy sweep of every `file:line` reference in the setup guide. Removed the obsolete "Remove Setup Route" post-setup step (the welcome/config route was deleted in the #458 hotfix and no longer exists in `dashboard.routes.ts`). Corrected the Database Configuration section: PostgreSQL is the default and already env-configured via `dbEnv.PG_CONNECTION_STRING` (the prior "hardcoded values" claim was wrong), and the incorrect "uncomment `drizzle-postgres-connector.ts`" step was dropped because that connector is already live; MySQL is now documented as an explicit opt-in (uncomment the `dialect: 'mysql'` and `url: dbEnv.MYSQL_CONNECTION_STRING` lines in `drizzle.config.ts`). Fixed the broken third-party wordlist link (`./src/api/…` → `./apps/reference-app/src/api/utils/wordlists/README.md`). Every remaining `file:N` citation was converted to a durable file + symbol/`TODO`-comment landmark so the setup docs stop re-staling on the next edit. D3 (the postinstall first-run note) was already resolved by [#491](https://github.com/ResetShop/angular-nx-standalone-starter/issues/491) and the eight-sub-schema rename by [#497](https://github.com/ResetShop/angular-nx-standalone-starter/issues/497) — neither is re-done here; no `docs/` files required changes. Documentation-only; no runtime change. ([#492](https://github.com/ResetShop/angular-nx-standalone-starter/issues/492))

### Security

- **CI sources `INTEGRATION_TEST_ADMIN_PASSWORD` from a masked Secret, and stale plaintext credential Variables were removed ([#508](https://github.com/ResetShop/angular-nx-standalone-starter/issues/508)).** `.github/workflows/ci.yml` now reads `INTEGRATION_TEST_ADMIN_PASSWORD` from `${{ secrets.* }}` in the `test-integration` and `e2e` jobs (previously `${{ vars.* }}`). Now that the repository is public, an Actions **Variable** is unmasked and can surface in public workflow logs, whereas a **Secret** is masked. The duplicate plaintext credential Variables `PG_CONNECTION_STRING` and `PASETO_SECRET_KEY` — which shadowed the existing Secrets and held real credentials — were deleted from repo settings and those credentials rotated. `PG_TEST_CONNECTION_STRING` intentionally stays a Variable (it targets the in-job `postgres:17` service at `localhost`, carrying no real credential). **Migration:** forks running the integration/e2e CI must provide `INTEGRATION_TEST_ADMIN_PASSWORD` as an Actions **Secret** (Settings → Secrets and variables → Actions → Secrets); a Variable of that name is no longer read. ([#508](https://github.com/ResetShop/angular-nx-standalone-starter/issues/508))

## [1.0.0] — 2026-06-04

First fully-featured release of the starter under the fork-distribution model — the consolidation of everything accumulated since `1.0.0-beta.3`. Closes the 1.0.0 milestone. Forks should read this section's `Migration:` notes before merging.

### Added

- **End-to-end Health-page coverage — final slice of the [#261](https://github.com/ResetShop/angular-nx-standalone-starter/issues/261) e2e epic (stacked on #465).** New `apps/reference-app/e2e/specs/health/health.spec.ts` + `page-objects/health.page.ts` covering `/dashboard/health`: the page renders the 'Application Health Checker' heading, the 'Checks' / 'Database' sections, and the status from the live `GET /api/health/v1` — with the overall status and the database check both reporting `healthy` against the seeded test DB. Read-only (no mocks); the route has no permission guard, so no deny case. Completes the e2e epic (#459–#466). Reuses the #459 harness; no production code changed. Runs via `npm run test:e2e` (outside `npm run ci`). ([#466](https://github.com/ResetShop/angular-nx-standalone-starter/issues/466))
- **End-to-end Theme & i18n coverage — slice of the [#261](https://github.com/ResetShop/angular-nx-standalone-starter/issues/261) e2e epic (stacked on #464).** New `apps/reference-app/e2e/specs/settings/theme-and-i18n.spec.ts` covering: the header theme toggle switching light↔dark (`<html class="dark">`) and **persisting across a reload** (via `localStorage['theme-preference']`), and the `/dashboard/settings` language selector switching the UI to Spanish (heading re-renders `Settings`→`Ajustes`) and persisting the choice to `localStorage['app_language']`. The language test asserts the persisted storage value rather than reloading, because the test fixture seeds `localStorage['app_language']='en'` via `addInitScript` before each page load (`page.goto`/`page.reload`); theme persistence uses a real reload (the fixture doesn't touch the theme key). Reuses the #459 harness; no production code changed. Runs via `npm run test:e2e` (outside `npm run ci`). ([#465](https://github.com/ResetShop/angular-nx-standalone-starter/issues/465))
- **End-to-end Permissions read-only coverage — slice of the [#261](https://github.com/ResetShop/angular-nx-standalone-starter/issues/261) e2e epic (stacked on #463).** New `apps/reference-app/e2e/specs/permissions/permissions-list.spec.ts` + `page-objects/permissions-list.page.ts` covering the read-only permissions view (`/dashboard/authorization/permissions`): the page renders (heading, the `module:resource:action` description, the data table), a seeded permission identifier (`admin:users:read`) is shown, and a no-permission user deep-linking the page is redirected with the access-denied toast. The permissions view is display-only (no create/edit/delete and no search control), so all tests read the real backend. Reuses the #459 harness; no production code changed. Runs via `npm run test:e2e` (outside `npm run ci`). ([#464](https://github.com/ResetShop/angular-nx-standalone-starter/issues/464))
- **End-to-end Roles CRUD coverage — slice of the [#261](https://github.com/ResetShop/angular-nx-standalone-starter/issues/261) e2e epic (stacked on #462).** New `apps/reference-app/e2e/specs/roles/roles-crud.spec.ts` + `page-objects/roles-list.page.ts` covering the roles admin (`/dashboard/authorization/roles`): list render, search, create via drawer (success toast), opening the edit drawer pre-filled with the role, delete of a removable role (confirm → success toast), the non-removable role exposing Edit but not Delete, and the permission-gated deny on deep-link. Create/delete success paths are mocked via method-guarded `page.route` (`/api/access/roles`) so the shared DB is never written. Reuses the #459 harness; no production code changed. Runs via `npm run test:e2e` (outside `npm run ci`). ([#463](https://github.com/ResetShop/angular-nx-standalone-starter/issues/463))
- **End-to-end Users coverage — slice of the [#261](https://github.com/ResetShop/angular-nx-standalone-starter/issues/261) e2e epic (stacked on #461).** New `apps/reference-app/e2e/specs/users/{users-list,user-detail}.spec.ts` + `page-objects/{users-list,user-detail}.page.ts` covering the users list (render, search, pagination, permission-gated deny on deep-link), the create drawer, delete-from-list, and the `/dashboard/users/:id` detail page (profile save, edit-roles drawer + admin-role lock on self-edit, account actions disable, danger-zone delete → redirect to the list, back link, self-view hiding the actions/danger sections, route-guard deny). Mutating success paths are mocked via method-guarded `page.route` so the shared e2e DB is never written; reads hit the real backend. Seeds a plain `viewable` target user + bulk users (to span more than one page) in `db-seed.ts`, and publishes the viewable/admin user IDs from `global-setup.ts` for direct detail-page navigation. Reuses the #459 harness; no production code changed. Runs via `npm run test:e2e` (outside `npm run ci`). ([#462](https://github.com/ResetShop/angular-nx-standalone-starter/issues/462))
- **End-to-end Dashboard-shell coverage — slice of the [#261](https://github.com/ResetShop/angular-nx-standalone-starter/issues/261) e2e epic (stacked on #460).** New `apps/reference-app/e2e/specs/dashboard/{dashboard-shell,dashboard-permissions}.spec.ts` + `page-objects/dashboard.page.ts` covering: sidebar nav rendering + breadcrumb, sidebar collapse/expand, the mobile navigation drawer, the authorization landing's Roles/Permissions cards, **permission-based sidebar filtering** (admin sees the Administration modules; the zero-permission user does not), the **no-module-access empty state** (`role="status"` "No module access" alert for the zero-permission user, absent for an admin, unguarded nav still present), and the **deep-link deny flow** (a forbidden route redirects to `/dashboard` with the `PERMISSIONS.ERRORS.ACCESS_DENIED` toast shown exactly once). Adds capture of the seeded zero-permission user's `storageState` in `e2e/setup/auth-setup.ts` (+ `STORAGE_STATE.noPermission`). Reuses the #459 harness; no production code changed. Runs via `npm run test:e2e` (outside `npm run ci`). ([#461](https://github.com/ResetShop/angular-nx-standalone-starter/issues/461))
- **End-to-end Landing-page coverage — slice of the [#261](https://github.com/ResetShop/angular-nx-standalone-starter/issues/261) e2e epic (builds on #459).** New `apps/reference-app/e2e/specs/landing/landing.spec.ts` + `page-objects/landing.page.ts` covering the public landing route `/`: anonymous render with no guard redirect, hero heading + the three feature cards, hero CTA → `/auth/login`, header theme-toggle + Sign-in link, the (sr-only) skip-to-content link, absence of the "Go to dashboard" link when logged out, and — using the seeded admin `storageState` — that a visitor with a session cookie is not redirected away. Writing this surfaced a product gap ([#468](https://github.com/ResetShop/angular-nx-standalone-starter/issues/468)): the header's "Go to dashboard" link is unreachable because the public route never validates the session, so the spec asserts the current behavior (link absent on cold load) and references #468. Reuses the #459 harness/fixtures; no production code changed. Runs via `npm run test:e2e` (outside `npm run ci`). ([#460](https://github.com/ResetShop/angular-nx-standalone-starter/issues/460))
- **End-to-end test foundation + Auth coverage (Playwright) — first slice of the [#261](https://github.com/ResetShop/angular-nx-standalone-starter/issues/261) e2e epic.** The reference app shipped a `playwright.config.ts` and an `e2e` target but no `e2e/` tree or specs; this establishes the harness all later epic slices (#460–#466) build on. New `apps/reference-app/e2e/` with: a Playwright `globalSetup`/`globalTeardown` that reuses the integration embedded-Postgres harness (`src/api/integration/setup/embedded-pg-test-db.ts`) — embedded Postgres locally, the CI `postgres:17` service via `PG_TEST_CONNECTION_STRING` — pushes the Drizzle schema, and seeds three fixture users (admin, a zero-permission user, and a `mustChangePassword` user) via relative-import Drizzle writes (no `@config`/`@schema` aliases, which Playwright's loader can't resolve); a `setup` project (`auth-setup.ts`) that logs each fixture user in and captures their authenticated `storageState` (gitignored under `e2e/fixtures/states/`); page objects for the four auth pages; and eight Auth specs covering login (success/invalid/validation/forgot-link), logout, transparent token refresh (synthetic 401 on `/api/auth/me`), protected-route + wildcard redirects, forgot-password (validation, neutral confirmation, 429 countdown), reset-password-confirm (missing-token gate, success → login), change-password (validation, wrong-current-password error, success → dashboard), and the `forcedPasswordChangeGuard` redirect (no loop). `playwright.config.ts` gains `globalSetup`/`globalTeardown`, a `setup` project with `dependencies: ['setup']` on the browser projects, `locale: 'en-US'`, and a 180s webServer timeout; a dedicated `e2e/tsconfig.json` is added; the CI `e2e` job gains a `postgres:17` service + `PG_TEST_CONNECTION_STRING`/`INTEGRATION_TEST_ADMIN_PASSWORD` env (mirroring `test-integration`). The e2e suite is exercised by the `e2e` target (`npm run test:e2e` / the CI `e2e` job) — it is intentionally outside `npm run ci`. No fork migration required — additive test infrastructure; forks inherit a working e2e starting point. ([#459](https://github.com/ResetShop/angular-nx-standalone-starter/issues/459))
- **`no-process-env` ESLint enforcement — PR B5 of the [#354](https://github.com/ResetShop/angular-nx-standalone-starter/issues/354) env-contract stack (the final PR; closes #354).** A `no-restricted-syntax` rule in `eslint.config.mjs` (new `processEnvRestrictedSyntax` selector, added to the main `no-restricted-syntax` array) blocks all `process.env.X` / `process.env['X']` reads, making the env contract structurally enforced rather than reviewer-dependent — mirroring the existing `vi.fn()` / `vi.mock()` enforcement pattern. A dedicated `no-process-env-allowlist` config lifts only the `process.env` restriction (keeping the common + vi restrictions) for the three file groups that legitimately read `process.env`: `apps/**/src/api/config/**` (the env sub-schemas and the `createEnvHandler` factory that parse `process.env` behind the lazy proxy, plus their specs), `apps/**/src/test-setup.ts` (pre-seeds env before production modules load), and `apps/**/src/api/integration/**` (integration setup helpers and specs that drive fixtures via env). The rule was verified to fire (a deliberate `process.env['TEST_VAR']` in a non-allowlisted file errors with the configured message pointing at `@config/<domain>.env`) and `npm run lint` passes with it active. `docs/environment-variables.md`'s Enforcement Summary row is updated from the `(Coming in a follow-up PR.)` placeholder to the live rule + allowlist. **Forks adding new `process.env` reads will fail `npm run lint` on the next upstream merge.** Migration: import the typed value from `@config/<domain>.env` (e.g. `dbEnv.PG_CONNECTION_STRING`, `authEnv.PASETO_SECRET_KEY`) or extend the allowlist in `eslint.config.mjs` with documented justification. ([#444](https://github.com/ResetShop/angular-nx-standalone-starter/issues/444))
- **Six domain-scoped env sub-schema modules + `createEnvHandler` factory + `@config/*` path alias + CI guard against `.env*` files in the working tree.** This is PR A of the stacked sequence that closes [#354](https://github.com/ResetShop/angular-nx-standalone-starter/issues/354) (the replacement of `.env`-file-based configuration with a Zod-validated env contract). New files under `apps/reference-app/src/api/config/`: `env-utils.ts` (the `createEnvHandler` factory — owns the lazy-init Proxy, the FATAL `process.exit(1)` formatting on Zod failure, the `seed` / `reset` test helpers, and the `formatZodError` formatter), `db.env.ts` (`PG_CONNECTION_STRING`, `PG_TEST_CONNECTION_STRING`), `auth.env.ts` (`PASETO_*`, `COOKIE_SECURE`, `AUTH_MAX_FAILED_ATTEMPTS`, `AUTH_LOCKOUT_DURATION`, `AUTH_CHANGE_PASSWORD_RATE_LIMIT_WINDOW`, `AUTH_CHANGE_PASSWORD_RATE_LIMIT_MAX`, `BCRYPT_COST`, `CRON_SECRET`), `email.env.ts` (`EMAIL_PROVIDER` + `SMTP_*` with `superRefine` cross-field validation requiring `SMTP_HOST` / `SMTP_USER` / `SMTP_PASS` when `EMAIL_PROVIDER=nodemailer`), `http.env.ts` (`BASE_HREF`, `IS_SERVERLESS`, `PORT`, `CORS_ORIGIN`, `CORS_MAX_AGE` + the `isServerless()` helper), `app.env.ts` (`NODE_ENV`, `APP_LANGUAGE`, `INTEGRATION_TEST_ADMIN_PASSWORD`), `cron.env.ts` (`TOKEN_CLEANUP_INTERVAL`, `TOKEN_CLEANUP_BATCH_SIZE`, `TOKEN_CLEANUP_MAX_BATCH_COUNT`). Each module exports four symbols (`parse<Domain>Env`, `<domain>Env`, `seed<Domain>Env`, `reset<Domain>Env`) and ships a focused `*.spec.ts` covering parsing, defaults, cross-field refinements, and the seed/reset/proxy lifecycle. The `@config/*` path alias is added to `apps/reference-app/tsconfig.json`, `apps/reference-app/tsconfig.spec.json`, and `apps/reference-app/vitest.config.ts`. New `scripts/check-no-env-files.mjs` fails the pre-commit hook AND `npm run ci` if any `.env*` file appears at the repo root or under `apps/*` — the legacy `.env.example` is removed (replaced by the new `docs/environment-variables.md` Variable Index). `.gitignore` broadened from `.env` to `.env` + `.env.*` plus a `tailwindcss-*.log` entry. New `.claude/settings.json` ships team-wide deny rules blocking AI coding agents from reading, writing, editing, or shell-creating `.env*` files. CLAUDE.md gains a new `## Environment Variables` section pointing at the sub-schema model and the four supported delivery mechanisms. This PR is **infrastructure only** — no production consumer was migrated to the new proxies and no `process.env` access was removed; that work is split across the follow-up stacked PRs (DI patterns → consumer migration → entry-point scripts → cleanup → ESLint enforcement). Forks should not need to take any action from this PR alone. ([#354](https://github.com/ResetShop/angular-nx-standalone-starter/issues/354))
- **Timer-based lockout & rate-limit countdown UX for the auth pages.** The three auth throttle surfaces now show a live "try again in mm:ss" countdown and disable submit until it elapses — entirely server-driven, no persistent client flags. **Backend:** `AuthError` carries an optional `lockedUntil`, threaded from the `checkAccountLockout` throw site so the login `401 ACCOUNT_LOCKED` body now includes `lockedUntil` (ISO-8601) via a new dedicated `loginErrorResponseSchema` (other auth endpoints' 401 shape is unchanged); the per-IP forgot-password and reset-password rate limiters already emit a standard `Retry-After` header (`hono-rate-limiter` draft-7), which the frontend consumes — no response-body change. **Frontend:** a shared `createCountdown(source)` primitive + `formatCountdown` (`apps/reference-app/src/app/pages/auth/countdown.ts`) derives a self-terminating, SSR-safe, teardown-managed seconds signal from a future ISO timestamp; `AuthState` gains `loginLockedUntil`, `loginThrottledUntil`, `resetThrottledUntil`, and `resetPasswordThrottledUntil` (set from the 401 `lockedUntil` / 429 `Retry-After`, cleared on a fresh attempt); the login, reset-password, and reset-password-confirm pages render the countdown and gate submit on it. The login page covers **both** triggers — the per-account ACCOUNT_LOCKED 401 lockout and the per-IP login 429 — through one unified countdown source, picking the appropriate translation ("too many failed attempts" vs "too many requests") for each. In practice the per-IP 429 is the path real users hit first on a shared client (both limits trip at 5/15m and the rate limiter fires before the handler), so without this the login page rendered the 429 as `AUTH.ERRORS.GENERIC` ("Error al iniciar sesión…") with no countdown. The forgot-password page keeps its optimistic confirmation (#433) and only **overlays** the countdown on a 429 — safe because the 429 is per-IP and fires before any account lookup, so it cannot leak account existence. New `AUTH.ERRORS.ACCOUNT_LOCKED_UNTIL` / `AUTH.ERRORS.RATE_LIMITED_UNTIL` en/es keys; `AccountLocked` / `Throttled` Storybook stories. Account lockout remains a **15-minute auto-unlock** (no admin action). Also fixes two pre-existing gaps surfaced by this work: `AUTH.ERRORS.ACCOUNT_LOCKED` was missing from the app `translation.mock.ts`, and `docs/api/auth/Login - Invalid Credentials.bru` asserted `res.body.error` instead of `res.body.code`. **Coverage:** unit (countdown primitive, store lockout/throttle wiring, all three page countdowns via fake timers), integration (`lockedUntil` in the ACCOUNT_LOCKED 401; 429 + `Retry-After` on all three endpoints — login, forgot-password, reset-password), and Bruno entries. **Migration:** forks that maintain their own translation files must add `AUTH.ERRORS.ACCOUNT_LOCKED_UNTIL` and `AUTH.ERRORS.RATE_LIMITED_UNTIL` to satisfy `TranslationSchema`; the login `401 ACCOUNT_LOCKED` body gains `lockedUntil` (additive); forks reading `LoginErrorResponse` get the new optional field. Implements #434 Concern B. ([#436](https://github.com/ResetShop/angular-nx-standalone-starter/issues/436))
- **Self-service forgot-password / reset flow.** The unauthenticated counterpart to the change-password flow (#388): a user who cannot log in requests a reset by email and sets a new password via a single-use token link. **Backend:** a new `password_reset_token` table (`apps/reference-app/src/db/schema/password-reset-token.ts`, registered in the integration schema push) storing only the **SHA-256 hash** of each token with an expiry (`PASSWORD_RESET_TOKEN_EXPIRY = '1h'`) and a single-use `usedAt` marker; a `DrizzlePasswordResetTokenRepository`; and a focused `PasswordResetService` (a sibling collaborator per the #420 model). `POST /api/auth/forgot-password` (public, rate-limited) **always returns 200** with a neutral message regardless of whether the email belongs to an active account (no user enumeration) — only active users get a token created and a reset-link email sent (new bilingual `forgot-password-email.builder.ts`; the email carries a link, never a password). `POST /api/auth/reset-password` (public, rate-limited) validates the token (missing/expired/used → **400 `RESET_TOKEN_INVALID`**, deliberately one code for all three so token state cannot be probed) and that the user is still active, then sets the new password + consumes the token inside a single transaction and revokes all of the user's refresh-token sessions. New `RESET_TOKEN_INVALID` internal+public error code; the shared `MIN/MAX_PASSWORD_LENGTH` bounds are reused. **Frontend:** `AuthApi.forgotPassword`/`resetPassword` (+ in-memory mock) and `AuthStore.forgotPassword`/`resetPassword` (`rxMethod`s with new `isRequestingReset` / `resetRequested` / `isResettingPassword` / `resetPasswordError` state); the existing `/auth/reset-password` page is wired to `forgotPassword` and now renders a neutral "if an account exists, a link was sent" confirmation (no enumeration); a new `/auth/reset-password/confirm` page (raw token read from the `?token=` query param, new-password form with the min-12 rule, surfaces `RESET_TOKEN_INVALID`) calls `resetPassword` and routes to login on success. en/es i18n: `AUTH.RESET_PASSWORD.{DESCRIPTION,CONFIRMATION}`, the new `AUTH.RESET_PASSWORD_CONFIRM.*` block, and `AUTH.ERRORS.RESET_TOKEN_INVALID`. **Coverage:** integration (forgot always-200 incl. unknown email + identical body; reset happy path / expired / used / unknown token / short password / session revocation), unit (service, repository mock, email builder, store), page + confirm-page specs + Storybook stories, and Bruno entries. **Migration:** the new `password_reset_token` table must be applied to fork databases (the integration suite pushes it from the schema files, as production schema application does); forks that maintain their own translation files must add the new `AUTH.RESET_PASSWORD.{DESCRIPTION,CONFIRMATION}`, `AUTH.RESET_PASSWORD_CONFIRM.*`, and `AUTH.ERRORS.RESET_TOKEN_INVALID` keys to satisfy `TranslationSchema`; the reset-link base URL is resolved from `CORS_ORIGIN` (the configured frontend origin) with a `http://localhost:4200` fallback. Builds on [#388](https://github.com/ResetShop/angular-nx-standalone-starter/issues/388) and [#420](https://github.com/ResetShop/angular-nx-standalone-starter/issues/420). ([#428](https://github.com/ResetShop/angular-nx-standalone-starter/issues/428))
- **Forced password-change flow after login (`mustChangePassword`).** Users flagged `mustChangePassword: true` (e.g. after an admin reset or first login) are now forced through a dedicated password change before reaching any protected route. **Backend:** new protected `POST /api/auth/change-password` (route in `apps/reference-app/src/api/modules/auth/auth.routes.ts`, handler in `auth.controller.ts`) requires the current password, validates the new one against the shared `MIN_PASSWORD_LENGTH` (12) / `MAX_PASSWORD_LENGTH` (128) bounds plus an old≠new refinement (`changePasswordRequestSchema`), and is rate-limited per IP (`changePasswordRateLimiter`, default 5 / 15m, overridable via `AUTH_CHANGE_PASSWORD_RATE_LIMIT_WINDOW` / `_MAX`). `AuthService.changePassword` runs a single transaction (`RefreshTokenRepository.runInTransaction`): `AuthPasswordService.changePassword` verifies the current password (throws `OLD_PASSWORD_MISMATCH` → **400**, not 401, so the frontend's 401-logout interceptor does not fire) and writes the new hash via `setPassword(..., mustChangePassword=false)`, then `revokeAllForUserExcept` invalidates every _other_ session while preserving the caller's current refresh token. `GET /api/auth/me` now returns `mustChangePassword` (via the new `AuthPasswordService.getMustChangePassword`, read fresh from the auth record) so the forced-change state survives a page reload — the short-lived access token does not carry it. The login timing-safety dummy hash is hardened from a hand-written malformed literal to a real, memoized bcrypt hash derived from the password module (`createPasswordHasher`), so the email-enumeration timing defense can never silently degrade. **Frontend:** `AuthStore.changePassword` (`rxMethod`, new `isChangingPassword` / `changePasswordError` state) backed by `AuthApi.changePassword`; a dedicated `/auth/change-password` page (signal-forms + `FormField`, min-12, surfaces `OLD_PASSWORD_MISMATCH`, navigates to the dashboard on success); a `forcedPasswordChangeGuard` that redirects must-change users to it (added after `authGuard` on the dashboard route — `canActivate` guards run sequentially, so it reads the store `authGuard.validateSession()` just populated with no extra `/me` round-trip); `validateSession()` now re-derives `mustChangePassword` on every navigation. The `auth/change-password` route is registered _before_ the `noAuthGuard`-guarded `auth` group, because a must-change user is authenticated and must not be bounced away as already-logged-in (which would loop against the dashboard guard). **Contracts / i18n:** new `OLD_PASSWORD_MISMATCH` internal+public error code (safe to expose — the caller is already authenticated, so it leaks nothing about other accounts), `MIN/MAX_PASSWORD_LENGTH` shared constants, and `AUTH.CHANGE_PASSWORD.*` + `AUTH.ERRORS.OLD_PASSWORD_MISMATCH` `TranslationSchema` keys (en/es). **Coverage:** integration tests for change-password (happy path, `OLD_PASSWORD_MISMATCH`, length validation, session fan-out revocation, unauthenticated), the `/me` flag, login enumeration-parity (byte-identical 401 for unknown email vs wrong password), and the dual-auth token-cleanup endpoint; unit tests for the new service/store methods; page + guard specs + a Storybook story; Bruno entries (`docs/api/auth/Change Password*.bru`, plus the updated `Get Current User.bru`). **Migration:** forks that maintain their own translation files must add `AUTH.CHANGE_PASSWORD` (`TITLE`, `DESCRIPTION`, `OLD_PASSWORD_LABEL`, `NEW_PASSWORD_LABEL`, `SUBMIT`) and `AUTH.ERRORS.OLD_PASSWORD_MISMATCH` to satisfy `TranslationSchema` (typecheck fails otherwise); the `GET /api/auth/me` payload gains `mustChangePassword` (additive); forks that hand-instantiate the auth services outside the Awilix container must provide the new `AuthService.changePassword` and `AuthPasswordService.{changePassword, getMustChangePassword}` methods. Builds on the [#420](https://github.com/ResetShop/angular-nx-standalone-starter/issues/420) `AuthService` decomposition. ([#388](https://github.com/ResetShop/angular-nx-standalone-starter/issues/388))
- **Public SSR landing page at `/`.** New root route (`path: ''`, `pathMatch: 'full'`, no guard) in `apps/reference-app/src/app/app.routes.ts` lazy-loads a new `LandingPage` (`apps/reference-app/src/app/pages/landing/landing.ts`) — a fully public, server-side-rendered presentation page with a hero section and a three-up grid of feature highlights (Authentication, RBAC, SSR). Because the route carries no `canActivate`, the existing server-route derivation in `app.routes.server.ts` renders it via `RenderMode.Server` automatically — **no change to `app.routes.server.ts` or its spec was required.** A new dedicated `LandingHeader` component (`apps/reference-app/src/app/components/landing-header/landing-header.ts`) provides the public-page header: a brand wordmark, the `ThemeToggle`, and a **Login** link to `/auth/login`; it deliberately does **not** reuse the dashboard `Header` (which is an `[appHeader]` attribute directive coupled to `UIStore` and the breadcrumb `Navigation`). Authenticated visitors are **not** redirected — the page is shown to everyone (satisfying "no guard required"), and the header surfaces an extra "Go to dashboard" link when `AuthStore.isAuthenticated()` is true. Both components ship with Angular Testing Library specs and `*.stories.ts`. A new `LANDING` namespace (`PAGE_TITLE`, `HERO_*`, `LOGIN_BUTTON`, `GO_TO_DASHBOARD`, `FEATURES.*`) is added to `TranslationSchema` (`packages/angular-core/src/lib/i18n/translations.schema.ts`), `en.ts`, `es.ts`, and the app test mock (`translation.mock.ts`). **Fork-visible behavior change:** visiting `/` previously fell through the `**` wildcard and redirected to `/auth/login`; it now serves the landing page (the wildcard is unchanged for all other unknown paths). **Migration:** forks that hand-maintain their own translation files will get a `TranslationSchema` compile error until they add the `LANDING` keys (copy from `en.ts`/`es.ts`; note that `LANDING.GO_TO_DASHBOARD` is removed later in this same release — see the `#468` entry under `### Fixed` — so a fork merging the full release should omit it); forks that relied on `/` redirecting to login should re-point that expectation at the landing route or remove the `''` route. ([#19](https://github.com/ResetShop/angular-nx-standalone-starter/issues/19))
- **`.claude/references/maintainability.md` — structural-simplification review lens, wired into `code-reviewer` and `refactoring-specialist`.** Resolves the [#378](https://github.com/ResetShop/angular-nx-standalone-starter/issues/378) / #409 evaluation of Cursor's `thermo-nuclear-code-quality-review` skill. That skill is **prompt-only** (a strict-maintainability review prompt, no executable code) and a strong _structural-simplification idea generator_, but **context-blind** as a standalone pass (it would contradict mandated project conventions — `rxMethod`, the two-`withMethods` store split, `find*()` naming, `Projection` types — on less-compliant code). Rather than add an external dependency, its missing-from-our-reviewer ideas (the "code judo" ambition to _delete_ complexity, spaghetti-conditional growth, thin/identity wrappers, diff-driven file bloat, avoidable sequential orchestration / non-atomic updates) are re-expressed in this project's voice in a new reference with explicit guardrails so it never overrides our hard constraints. `code-reviewer` now loads **13** references (the new lens included; full-load, ungated) and `refactoring-specialist` adds it to its fixed set; CLAUDE.md "Agent Reference Loading" is updated accordingly. The standalone evaluation report is **not** retained in `docs/` — its conclusion is embodied directly in this reference; the broader agentic-review-quality work continues separately. Agent-config/doc-only; no runtime, schema, or fork-API impact. ([#409](https://github.com/ResetShop/angular-nx-standalone-starter/issues/409))
- **`docs/NX_CLOUD.md` — Nx Cloud remote caching setup runbook + DTE evaluation.** New documentation covering how to enable Nx Cloud remote caching (so task outputs restore across the ephemeral web-session containers, local dev, and CI), the required `NX_CLOUD_ACCESS_TOKEN` and network-egress (`cloud.nx.app`) setup, the exact `.github/workflows/ci.yml` edits to apply once the workspace is claimed (remove `--skip-nx-cache` from cacheable jobs; inherit the token via top-level `env:`), and a distributed-task-execution (DTE / Nx Agents) evaluation (verdict: adopt remote cache + `nx affected` on PRs; defer DTE at the current 8-project / ~50s-cold-build size). **Important finding from live verification:** the `nxCloudId` in `nx.json` (`68ee91eecec0875b0f57a8d2`) points to an Nx Cloud workspace that was **never claimed** and now returns `401` ("Workspaces must be connected within 3 days of creation"), and no `NX_CLOUD_ACCESS_TOKEN` exists in any reachable environment — so remote cache hits cannot yet be demonstrated. Nx falls back to the working **local** cache, so nothing is broken; remote caching activates with zero further code changes once the repo owner claims the workspace and provisions the token per the runbook. A pointer was added to CLAUDE.md's "Local CI Verification" section. Documentation-only; no runtime, schema, or fork-API impact. ([#406](https://github.com/ResetShop/angular-nx-standalone-starter/issues/406))
- **`npm run ci:verify` — cache-aware CI for the agent inner loop.** New `package.json` script that runs the identical task set as `ci` (`stylelint`, `lint`, `typecheck`, then `test`, `test-integration`, `build`, `build-storybook`) but **without** `--skip-nx-cache`, so repeated runs on a largely-unchanged tree restore from the Nx local cache (and, once remote caching lands per [#406](https://github.com/ResetShop/angular-nx-standalone-starter/issues/406), the Nx Cloud remote cache) instead of forcing a full cold rebuild. The cold `npm run ci` is **unchanged** and remains the authoritative final gate — it keeps `--skip-nx-cache` to guarantee correctness independent of cache state. This removes the redundant cold-rebuild tax from the 2–3 intermediate CI runs the `/issue-workflow` skill performs per issue (Phase 4 orchestrator, inside `code-reviewer`, Phase 5 post-fix) while preserving a strict cold run for the release gate. CLAUDE.md "Common Commands" and "Local CI Verification" document both paths and the rule that `ci:verify` is for intermediate runs only, never the final gate. No new `.claude/settings.local.json` allow-rule was needed: the pre-existing `Bash(npm run ci:*)` rule already matches `npm run ci:verify`. No fork migration required — `ci` behaves exactly as before; `ci:verify` is purely additive. ([#404](https://github.com/ResetShop/angular-nx-standalone-starter/issues/404))
- **`<app-row-actions-menu>` component and table row-action migration.** New `packages/ui/src/lib/row-actions-menu/row-actions-menu.ts` exports a `RowActionsMenu` component that replaces inline action buttons in data-table rows with a single vertical-ellipsis (⋮) trigger that opens a popover menu. Built on `ng-primitives` `NgpMenuTrigger` / `NgpMenu` / `NgpMenuItem` — standard ARIA menu keyboard handling (arrow keys, Enter/Space, Escape) and overlay-portal placement (`bottom-start` with auto-flip). Item shape `RowAction { label, onSelect, variant?, icon?, disabled? }`; destructive items get `text-destructive` on the menu item itself (each item is its own popover element, so there is no cascade-order trap to navigate). Renders nothing when `actions` is empty — consumers do not need to guard. The `actions` input accepts either a flat `RowAction[]` (single group, no separators) or a matrix `RowAction[][]` (groups, with a `role="separator"` divider rendered between every pair of non-empty groups via `NgpSeparator`); empty groups are dropped automatically so consumers can build groups conditionally without producing dangling separators. Both `users-list` and `roles-list` pass groups so the destructive Delete action visually separates from the non-destructive group. The `triggerLabel` input defaults to `'Actions'`; apps pass a translated label via the new `ROW_ACTIONS.TRIGGER_LABEL` key (added to `translations.schema.ts`, `en.ts`/`es.ts`, the test mock, and the schema-fixture spec). The users-list and roles-list pages migrate their table action cells to the new component — three actions (Edit / Reset password / Delete) for users-list, two (Edit / Delete) for roles-list — with self-row filtering on Reset Password and Delete in users-list and `row.removable` gating on Delete in roles-list now expressed as imperative `getRowActions(row)` / `getRoleActions(row)` helpers (the per-row action list cannot use `*hasPermission` structural directives; the helpers read `currentUser()` from the same `AuthStore` signal). Mobile cards (`user-card`, `role-card`) are intentionally unchanged and keep their inline action area — a card's footprint accommodates inline buttons without row-stacking clutter. Storybook stories cover `Default`, `Open` (auto-opens via `play()`), `WithIcons`, `SingleAction`, and `EmptyActions`. Test infrastructure note: `packages/ui/src/test-setup.ts` gains a one-line `Element.prototype.getAnimations` polyfill because happy-dom omits the Web Animations API that ng-primitives' overlay teardown calls; without it every test using a menu reports an unhandled `TypeError` even though assertions pass. A previous attempt to solve the row-action clutter via a `ghost-muted` Button variant (issue [#386](https://github.com/ResetShop/angular-nx-standalone-starter/issues/386), PR [#399](https://github.com/ResetShop/angular-nx-standalone-starter/pull/399)) was closed without merging, so this PR starts from a clean `main` with no residue to revert — **no migration required for forks**. ([#401](https://github.com/ResetShop/angular-nx-standalone-starter/issues/401))
- **Strategic DDD patterns documented in `.claude/references/domain-model.md`.** Appends a new _Strategic Patterns_ part (Aggregate Design, Domain Events, Type-State Pattern, Policies as Pure Functions, Bounded Context Principles + Ubiquitous Language, Consistency Decisions) complementing the file's existing tactical patterns. Every example references the project's real domain types (`User`/`IUser`, `IRole`, `IPermission`, `IManagedUser`, `UserStatus`, `permission()`/`isPermissionName()`) — no Java/Kotlin library code. Agent-facing reference only: no runtime code, schema, or contract change; forks consuming the references inherit the expanded guidance on a merge. Implementation follow-ups for the documented patterns are tracked separately. ([#207](https://github.com/ResetShop/angular-nx-standalone-starter/issues/207))
- **Admin-initiated password reset.** New `POST /api/user/{id}/reset-password` endpoint (route in `apps/reference-app/src/api/modules/user/user-management.routes.ts`, handler in `user-management.controller.ts`) guarded by the existing `admin:users:reset_password` permission. The service (`UserManagementService.resetPassword`) generates a new temporary password server-side via the existing `generatePassword()`, bcrypt-hashes it, persists it through a new `UserManagementRepository.updatePasswordAndMustChange(id, hash, mustChangePassword)` repository method (transactional UPDATE on the `authentication` table, skips soft-deleted users), forces `mustChangePassword: true`, and emails the new password to the user. **The generated password is never returned in the response or written to any log** — the response is `{ message, passwordEmailSent }` (new `resetPasswordResponseSchema` / `ResetPasswordResponse` in `src/contracts/user/`), and a `logger.security('user_password_reset', { userId, actorId, passwordEmailSent })` audit event records the acting admin. A new bilingual `apps/reference-app/src/api/services/email/reset-password-email.builder.ts` renders the email (EN/ES, sentence-case Spanish, HTML-escaped); shared helpers `escapeHtml`/`resolveEmailLanguage` and the `EmailContent`/`EmailLanguage` types were extracted into a new `email-builder.utils.ts` consumed by both the welcome and reset builders. Frontend: `UsersApi.resetPassword` (HTTP + in-memory mock) feeds `UsersStore.resetPassword` (`rxMethod`, new `isResettingPassword` state and `mutationError.resetPassword` key); the action is exposed as a "Reset Password" row action in the users-list table and the mobile `user-card`, gated by `*hasPermission="'admin:users:reset_password'"`, behind a confirmation dialog with a success toast. New i18n keys `USERS.PAGE.RESET_PASSWORD_BUTTON`, `USERS.PAGE.RESET_PASSWORD_DIALOG.{TITLE,MESSAGE}`, and `USERS.RESET_PASSWORD_TOAST` added to `translations.schema.ts`, `en.ts`, `es.ts`, and the test mocks. Coverage: integration tests (200/404/401/403), service + controller + email-builder unit tests, store + component tests, and Bruno docs (`docs/api/user/Reset Password.bru`, `Reset Password - Not Found.bru`). **No fork-visible breaking change** — the endpoint, store method, and UI action are all additive. Note: the issue specified the (not-yet-built) user detail page #212 as the button location; the action was placed in the existing users-list row actions / mobile card instead. **Self-target hardening:** `UserManagementService.resetPassword(id, currentUserId)` now rejects admin-targets-self with the existing `SELF_LOCKOUT` error (controller emits 403 + `logger.security('self_lockout_blocked', { actorId, operation: 'user_password_reset' })`); the row-level Reset Password action is also hidden in the users-list and the mobile `user-card` when `row.id === authStore.currentUser()?.id`, matching the pattern already established for `deleteUser` / `updateUserStatus`. ([#214](https://github.com/ResetShop/angular-nx-standalone-starter/issues/214))
- **`@resetshop/generators:ui-component` Nx generator** — scaffolds a shared UI library component under `packages/ui/src/lib/<name>/` with all project conventions pre-filled: `ChangeDetectionStrategy.OnPush`, `app-` element selector, `@reference "#tailwind-theme"` in styles, `clearAllMocks()` in `beforeEach`, and `parameters.docs.canvas.sourceState: 'shown'` in the Storybook meta. Inputs: `name` (required), `directory` (default `packages/ui/src/lib`), `exportFromIndex` (boolean, default `true`), `inlineTemplate` (boolean, default `true`), `inlineStyle` (boolean, default `true`). When `inlineTemplate=false` the generator emits a sibling `<name>.html` and switches the decorator to `templateUrl`; when `inlineStyle=false` it emits a sibling `<name>.css` (pre-seeded with `@reference "#tailwind-theme"`) and switches to `styleUrl`. When `exportFromIndex=true` (default) the generator appends `export { <Class> } from './lib/<kebab>/<kebab>'` to `packages/ui/src/index.ts` with an idempotency guard. Invocation: `nx g @resetshop/generators:ui-component tooltip [--inlineTemplate=false] [--inlineStyle=false]`. The generators index in `packages/generators/generators.json` now exposes eight generators; `.claude/references/generators.md` and the README `Generators` table reflect the addition. Spec: `packages/generators/src/generators/ui-component/index.spec.ts` (15 tests covering substitution, sidecar emission, kebab-casing of compound names, idempotent index append, all four flag combinations, and a flag-invariance assertion on `.spec.ts`/`.stories.ts`). ([#348](https://github.com/ResetShop/angular-nx-standalone-starter/issues/348))
- **`custom-storybook/storybook-source-state` ESLint rule** (`packages/ui/eslint/storybook-source-state.js`) — fails CI when any `*.stories.ts` file omits `parameters.docs.canvas.sourceState: 'shown'`. Walks the `ExportDefaultDeclaration` and resolves both inline-object exports (`export default { ... }`) and variable-referenced exports (`const meta = { ... }; export default meta`); cross-module imports of meta objects are silently skipped (safer than false-positives). Reports five distinct message IDs (`missingParameters`, `missingDocs`, `missingCanvas`, `missingSourceState`, `wrongSourceState`) for precise diagnostics. Registered in `eslint.config.mjs` under a new `storybook-source-state` named config block targeting `**/*.stories.ts`. Covered by a `RuleTester` spec at `packages/ui/eslint/storybook-source-state.spec.js` (8 cases — 3 valid, 5 invalid). **Forks with `*.stories.ts` files that omit `sourceState: 'shown'` will start failing `npm run lint` on the next upstream merge.** Migration: add `parameters: { docs: { canvas: { sourceState: 'shown' } } }` to each affected meta (same shape the `ui-component` generator scaffolds). ([#348](https://github.com/ResetShop/angular-nx-standalone-starter/issues/348))
- **`/issue-workflow` Claude Code skill** (`.claude/skills/issue-workflow/SKILL.md`) — orchestrates the full 6-phase issue resolution lifecycle (Setup → Plan → Implement → Review → Fix → Ship) as a single repeatable command invoked with `/issue-workflow <issue-url>`. Derives branch name from the issue title via `gh issue view`, delegates to `plan-writer` and `code-reviewer` agents at the appropriate phases, enforces atomic commits with `[#<issue>] - <description>` messages, and pauses for user approval after Plan and Review. Hard constraints embedded: no direct `nx` commands, no `cd`-prefixed git, no PR before CI + review, no skipped Plan phase. Documented in CLAUDE.md under "Issue Workflow Skill". ([#349](https://github.com/ResetShop/angular-nx-standalone-starter/issues/349))
- **Interactive skill installer** (`scripts/setup-skills.mjs`) — postinstall hook that auto-installs third-party Claude Code skills on `npm install`. In interactive terminals, presents an Nx-style multi-select prompt (arrow-key navigation, spacebar toggle, enter to confirm); in CI/piped environments, auto-installs all default-selected skills silently. Supports two install strategies: `clone` (shallow git clone into `.claude/skills/<name>/`) and `command` (runs a shell command, e.g. `npx nx configure-ai-agents`). Currently ships with the Angular skill (`angular/skills`); Nx skill deferred to [#351](https://github.com/ResetShop/angular-nx-standalone-starter/issues/351) pending README reconciliation. ([#349](https://github.com/ResetShop/angular-nx-standalone-starter/issues/349))

### Changed

- **Dashboard sidebar navigation regrouped, plus a Dashboard home link.** In `apps/reference-app/src/app/pages/dashboard/dashboard.navigation.ts` the **Settings** nav item moves from the top (`home`) section into the **"Settings & Maintenance"** section (alongside Health), and a new **Dashboard** link to `/dashboard` (labelled via the existing `DASHBOARD.BREADCRUMB` key — `Dashboard` / `Panel principal`) is added to the top section. To avoid a card that links the dashboard to itself, `dashboard-home.ts` now renders its landing cards from a `cardSections` computed that filters out the self-referential `/dashboard` route and drops any section left empty by that filter. **Migration:** none — UI navigation only, no API/schema/contract change.
- **Align the user API to plural collection routes: `/api/user*` → `/api/users*` ([#455](https://github.com/ResetShop/angular-nx-standalone-starter/issues/455)).** The user module was the only backend collection mounted in the singular (`/api/user`), disagreeing with its siblings `/api/access/roles` and `/api/access/permissions`, the frontend routes (`/dashboard/users`), and the standard REST convention of plural nouns for collections. The mount in `apps/reference-app/src/api/routes.ts` changes from `/user` to `/users`, so every endpoint moves: `GET|POST /api/users`, `GET|PATCH|DELETE /api/users/{id}`, `PATCH /api/users/{id}/status`, `POST /api/users/{id}/reset-password`, and the user-role sub-resources `GET|POST|PUT|DELETE /api/users/{userId}/roles*` and `GET /api/users/{userId}/permissions`. The frontend `HttpUsersApi` base path, the user + user-role integration/provider specs, the controller doc-comments, the Bruno client (`docs/api/user/*.bru`, `docs/api/user-role/*.bru`), and `.claude/references/backend-api.md` are updated to match. Route-definition `tags:` and the relative `createRoute` paths are unchanged (they ride the mount). **Migration (fork-visible breaking URL change):** any consumer calling `/api/user*` must update to `/api/users*`; there is no aliasing of the old paths. Done at milestone 1.0.0 to minimize pinned consumers. ([#455](https://github.com/ResetShop/angular-nx-standalone-starter/issues/455))
- **User detail page refinements: working role edits, admin self-lockout guards, localized status, best-effort reset-password email, and permission-gated actions card ([#212](https://github.com/ResetShop/angular-nx-standalone-starter/issues/212)).** Follow-up fixes on the new `/dashboard/users/:id` detail page. (1) **Role edits now persist.** `PATCH /api/user/{id}` previously advertised `roleIds` in its contract but `UserManagementService.updateUser` dropped it, so the Edit Roles drawer didn't save. `updateUser` now replaces the user's roles via `userRoleRepository.replaceUserRoles` (audit-diffed) when `roleIds` is provided. (2) **Admin self-lockout is blocked** both in the UI (the admin role checkbox is locked-on in the Edit Roles drawer when an admin edits their own roles — new `lockedRoleIds` input on `RoleSelector`, shared `ADMIN_ROLE_CODE` constant in `@contracts/role/role.constants`) and in the backend (new `SELF_ADMIN_REMOVAL` error → `403` when an admin's own update would remove their admin role). (3) **Best-effort reset-password email.** `POST /api/user/{id}/reset-password` now responds as soon as the password is reset and dispatches the temporary-password email **after** the response via `deferAfterResponse`, so the client toast reflects the completed reset rather than the SMTP round-trip; the toast copy now says the password "is being emailed". `ResetPasswordResponse` **no longer includes `passwordEmailSent`** (delivery is unknown at response time). (4) **Localized status pill.** The users-list cell and mobile card rendered the raw capitalized status enum (always English); both now use a shared `UserStatusBadge` that renders the translated `COMMON.STATUS.*` label, matching the detail page. (5) **Fresh detail view.** `updateUser` / `updateUserStatus` now reload `selectedUser` after success, so the detail page's status badge and roles reflect the change (previously only the list was reloaded). (6) **Permission-gated actions card.** The "Account Actions" card is hidden entirely when no action is available (self-view or missing both `admin:users:reset_password` and `admin:users:disable`) instead of rendering an empty titled shell. (7) **Delete navigates back to the list.** Deleting the viewed user now routes back to `/dashboard/users` on success. The post-delete reaction (success toast + navigation) moved from `UserDangerZone` up to the surviving `UserDetailPage` — a successful delete nulls `selectedUser`, which tore down the danger-zone subtree before its own effect could navigate; `UserDangerZone` is now presentational and emits a `deleteConfirmed` output that the page orchestrates. **Migration:** forks reading `passwordEmailSent` from the reset-password response must drop it — the field is gone; the email is sent best-effort after the response. ([#212](https://github.com/ResetShop/angular-nx-standalone-starter/issues/212))
- **Align consumer docs with the `@config` env contract — PR B4 of the [#354](https://github.com/ResetShop/angular-nx-standalone-starter/issues/354) env-contract stack.** `docs/AUTHENTICATION.md` (the env-var table now points at `docs/environment-variables.md`; the CORS code example reflects the lazy-init `httpEnv` middleware instead of the old eager `process.env` read; the troubleshooting section references the proxy FATAL message), `README.md` (the backend Environment Variables section, the auth-config setup, and the optional Database/Sanity/Clarity connector steps no longer reference an in-tree `.env` file or the deleted `packages/hono-core/src/lib/environment.ts` — they point at `docs/environment-variables.md`, the `@config/*` sub-schemas, and the connector templates), and `docs/DEPENDENCY_INJECTION.md` (the "validate configuration early" best practice now describes the env-proxy + `verifyContainer()` mechanism). Bruno API docs were audited and contained no env references. ([#443](https://github.com/ResetShop/angular-nx-standalone-starter/issues/443))
- **Gate entry-point scripts and drizzle-kit on `@config/*` via a tsx wrap — PR B3 of the [#354](https://github.com/ResetShop/angular-nx-standalone-starter/issues/354) env-contract stack.** The `drizzle:create-migrations` and `drizzle:push-migrations` npm scripts now run drizzle-kit through `tsx --tsconfig apps/reference-app/tsconfig.json ./node_modules/drizzle-kit/bin.cjs <subcommand>` so the `@config/*` (and `@contracts/*`) path aliases resolve when `drizzle.config.ts` loads — also clearing the latent `Cannot find module '@contracts/...'` introspection warning. `drizzle.config.ts` now reads `dbEnv.PG_CONNECTION_STRING` from `@config/db.env` instead of `process.env['PG_CONNECTION_STRING']`. Combined with B2's migration of `seed.ts` / `sync-permissions.ts` to `dbEnv`, all four database scripts (and the migration runner) now **fail fast at boot** with the formatted `FATAL: Environment validation failed (db domain)` message and `process.exit(1)` when `PG_CONNECTION_STRING` is missing — before any Drizzle/Postgres connection is attempted. A `db.env.spec.ts` test covers the proxy `process.exit(1)` path; `docs/environment-variables.md` documents the script fail-fast behavior. The integration `test-integration` job is unaffected — it pushes the schema programmatically via `drizzle-kit/api`'s `pushSchema`, never the npm scripts. **No fork-visible HTTP-contract, schema, or endpoint change.** **Migration:** forks that invoke `drizzle-kit generate`/`push` outside these npm scripts must add the same `tsx --tsconfig apps/reference-app/tsconfig.json ./node_modules/drizzle-kit/bin.cjs` wrap (or otherwise make the `@config/*` alias resolvable) and ensure `PG_CONNECTION_STRING` is delivered before invocation. ([#442](https://github.com/ResetShop/angular-nx-standalone-starter/issues/442))
- **Migrate all production `process.env` reads to `<domain>Env` proxies; delete `src/api/environment.ts`; collapse duplicate `isServerless()` — PR B2 of the [#354](https://github.com/ResetShop/angular-nx-standalone-starter/issues/354) env-contract stack.** Every `process.env[X]` read in production code outside `@config/*.env.ts` is replaced by the appropriate `<domain>Env.X` call: `cron-jobs.ts` (→ `authEnv.CRON_SECRET`, `cronEnv.TOKEN_CLEANUP_INTERVAL`), `refresh-token.repository.ts` (→ `cronEnv.TOKEN_CLEANUP_BATCH_SIZE` / `_MAX_BATCH_COUNT`), `rate-limit.middleware.ts` (→ `authEnv.AUTH_CHANGE_PASSWORD_RATE_LIMIT_WINDOW/_MAX`), `password-reset.service.ts` (→ `httpEnv.CORS_ORIGIN`), all three email builders (→ `appEnv.APP_LANGUAGE`), `nodemailer.repository.ts` (→ `emailEnv.*`), `container.ts` (→ `emailEnv.EMAIL_PROVIDER`), `password-hasher.ts` (→ `authEnv.BCRYPT_COST`), and `server.ts` (→ `httpEnv.PORT`). `apps/reference-app/src/api/environment.ts` (which re-introduced bare `process.env` reads after PR #398) is deleted; `src/db/seed.ts` and `src/db/sync-permissions.ts` read `dbEnv.PG_CONNECTION_STRING` from `@config/db.env` instead. `packages/hono-core/src/lib/runtime.ts` (a duplicate `isServerless()` reading `process.env['IS_SERVERLESS']` directly) and its spec are deleted, the `@resetshop/hono-core` re-export is removed, and the two consumers import `isServerless` from `@config/http.env`. The `NodemailerRepository` constructor's manual `parseInt` / missing-credentials guards are removed — the `emailEnv` schema's `superRefine` (boot-time `process.exit(1)`) provides the equivalent guarantee at a higher layer. Spec files that mutated `process.env['BCRYPT_COST']` / `process.env['APP_LANGUAGE']` switch to `seedAuthEnv`/`resetAuthEnv` and `seedAppEnv`/`resetAppEnv`. The three commented connector templates reference `@config/*.env` rather than the deleted `environment.ts`. After this PR, `grep -rE "process\.env\["` in `src/api/` returns hits only in `config/*.env.ts` and `container/validate-environment.ts` (the latter is intentionally left for B4). **No fork-visible HTTP-contract, schema, or endpoint change.** **Migration:** forks importing `isServerless` from `@resetshop/hono-core` must switch to `apps/<slug>/src/api/config/http.env`; forks importing `environment` from `src/api/environment.ts` must switch to `dbEnv.PG_CONNECTION_STRING` (`@config/db.env`) and `httpEnv.BASE_HREF` (`@config/http.env`). ([#441](https://github.com/ResetShop/angular-nx-standalone-starter/issues/441))
- **DI patterns enabling per-test env values — PR B1 of the [#354](https://github.com/ResetShop/angular-nx-standalone-starter/issues/354) env-contract stack (internal backend refactor).** Introduces the dependency-injection seams that let consumers move off direct `process.env` reads (the mechanical substitution lands in PR B2). Five changes: (1) **`PasetoConfig` extracted** — new `apps/reference-app/src/api/services/paseto/paseto.config.ts` declares the `PasetoConfig` interface (`secretKey`, `issuer`, `accessTokenExpiry`, `refreshTokenExpiry`, `clockTolerance`) and a `createPasetoConfig(source?: AuthEnv)` factory mapping `authEnv` fields; `PasetoService` (`paseto.service.ts`) now takes `{ pasetoConfig }` via constructor injection and reads zero `process.env` values; `paseto.service.spec.ts` drives every case (key rotation, expiry, issuer-mismatch) via constructor-injected configs with no env mutation. (2) **`AuthConfig.cronSecret` added** — `auth.config.ts` gains a `cronSecret: string | undefined` field, and `createAuthConfig(source?: AuthEnv)` now maps all fields (including `cronSecret`) from the validated `authEnv` source (the hand-rolled `parseMaxFailedAttempts` / `parseLockoutDuration` helpers and the `parseDurationToMs` import are deleted — the Zod schema already enforces those); the cleanup-tokens handler in `auth.controller.ts` reads `container.cradle.authConfig.cronSecret` instead of `process.env['CRON_SECRET']`; `auth.controller.spec.ts` drives the set/unset/mismatched cases via `InMemoryContainer` overrides. (3) **`DrizzlePgConnector` is now a factory** — `helpers/drizzle-postgres-connector.ts` exports `createDrizzlePgConnector()` reading `dbEnv.PG_CONNECTION_STRING` lazily on first call rather than at module-eval; registered via `asFunction(...).singleton()`. (4) **`cors()` deferred to first request** in `server.ts` via a lazy-init closure reading `httpEnv.CORS_ORIGIN` / `httpEnv.CORS_MAX_AGE`, so importing the bundle (e.g. the Angular SSR prerender worker, env-less) no longer triggers an env read. (5) **`generatePassword(wordCount?, language?)`** gains an optional `language` parameter defaulting to `appEnv.APP_LANGUAGE`, letting specs select a wordlist without env mutation. The `db`, `authConfig`, and `pasetoConfig` container registrations are unified on `asFunction(...).singleton()` for lazy resolution. **No fork-visible HTTP-contract, schema, or endpoint change.** **Migration:** forks that hand-instantiate `new PasetoService()` outside the container must pass `{ pasetoConfig: createPasetoConfig() }`; forks importing the `drizzlePgConnector` value must switch to calling `createDrizzlePgConnector()`. ([#440](https://github.com/ResetShop/angular-nx-standalone-starter/issues/440))
- **Decompose `AuthService` into `AuthService` + `TokenMaintenanceService` + `AuthPasswordService` (internal backend refactor).** The accreting `AuthService` (`apps/reference-app/src/api/modules/auth/auth.service.ts`) is split into three focused collaborators: `AuthService` keeps the core orchestration (`authenticate`, `refreshToken`, `logout`); the new `TokenMaintenanceService` (`token-maintenance.service.ts`) owns `cleanupExpiredTokens`; the new `AuthPasswordService` (`auth-password.service.ts`) owns password-credential verification (`validateCredentials` plus failed-login/lockout tracking), which `AuthService.authenticate` now delegates to. This establishes a seam for future non-password auth mechanisms (OAuth/SSO/passkeys) as sibling collaborators. `bcryptjs` is now fully encapsulated behind `apps/reference-app/src/api/services/password/password-hasher.ts`: a new `createPasswordVerifier()` factory (mirroring `createPasswordHasher`) backs a `verifyPassword` Awilix value, and the previously-inline `bcryptjs.compare` in the auth flow plus the integration-test seeding (`integration/setup/db-helpers.ts`) now route through the password module — `password-hasher.ts` is the **sole** `bcryptjs` importer. Container wiring (`container.ts` / `container.types.ts`): `tokenMaintenanceService` is now a real `asClass(TokenMaintenanceService)` registration (previously an `asFunction` alias of the `AuthService` instance), `authPasswordService` is a new cradle key, and the `Cradle.authService` type narrows from `AuthService & TokenMaintenanceService` to `AuthService`. The `auth.service.spec.ts` cleanup and password tests are split into `token-maintenance.service.spec.ts` and `auth-password.service.spec.ts`. **No fork-visible HTTP-contract change** — all endpoints, response shapes, and cookies are unchanged, verified by the unchanged auth integration suite. **Migration:** forks that hand-instantiate these services outside the container must update constructor deps — `AuthService` now requires an `authPasswordService` dep (and no longer takes a bcrypt/`verifyPassword` dep directly); `AuthPasswordService` requires `{ authRepository, verifyPassword }`; `TokenMaintenanceService` requires `{ refreshTokenRepository }`; obtain `verifyPassword` from `createPasswordVerifier()` in `password-hasher.ts`. ([#420](https://github.com/ResetShop/angular-nx-standalone-starter/issues/420))
- **Nx Cloud remote caching activated in CI (`.github/workflows/ci.yml`).** Completes the runbook drafted in `docs/NX_CLOUD.md` (#406) now that the workspace is claimed (PR #421 set `nxCloudId` `6a17a0e34cc7a0ffaeec195e`) and an `NX_CLOUD_ACCESS_TOKEN` secret exists. The workflow now inherits `NX_CLOUD_ACCESS_TOKEN` via a top-level `env:` block, and `--skip-nx-cache` is removed from the six cacheable jobs (`test`, `typecheck`, `lint`, `stylelint`, `build`, `storybook`) so they read/write the Nx Cloud remote cache; `e2e` stays cold (environment-sensitive, lowest cache value) and `test-integration` (run via `npm run test:integration`) is unchanged. The cold local `npm run ci` gate keeps `--skip-nx-cache` by design. **Migration for forks:** connect your own Nx Cloud workspace (`nxCloudId`) and set a **valid** `NX_CLOUD_ACCESS_TOKEN` GitHub Actions secret with the token **verbatim from the dashboard** (an opaque base64-style string — do not encode/decode it). Note the two distinct behaviors: a **missing** token degrades to the local cache (no failure), but a **present-but-invalid/mismatched** token is **fatal** — Nx returns `401` and the cacheable jobs exit non-zero. So either provision a correct token or revert this activation; a wrong value breaks CI rather than silently degrading. `docs/NX_CLOUD.md` is updated from "wired but not active" to "active" (verified by a green CIPE on PR #417's CI). ([#406](https://github.com/ResetShop/angular-nx-standalone-starter/issues/406))
- **Documented pipelines now run independent advisor agents in parallel.** CLAUDE.md "Agent Orchestration" / "Common Pipelines" now mark two advisor groups as concurrent (`∥`): the planning advisors `architecture-advisor` ∥ `domain-model-advisor`, and the pre-review advisors `test-generator` ∥ `security-auditor`. Each group is invoked as a single message with multiple `Agent` calls (i.e. the parallel `Agent` tool calls are issued in the same assistant turn). The "New feature" pipeline is rewritten as `(architecture-advisor ∥ domain-model-advisor) → implement → (test-generator ∥ security-auditor) → code-reviewer`. A "Parallel vs sequential" note documents the correctness rule — parallel agents must have **no shared write target** (agents that consume each other's output or write the same file, e.g. `workspace/PLAN.md`, stay sequential) — and clarifies that the `/issue-workflow` skill's own phase pauses (after Plan, after Review) remain sequential; only the intra-phase advisor fan-out is parallelized. Documentation-only; no runtime, schema, or fork-API impact. ([#408](https://github.com/ResetShop/angular-nx-standalone-starter/issues/408))
- **Planning agents (`plan-writer`, `architecture-advisor`) now load references conditionally (core always + diff-relevant domain refs), behind fail-open guardrails.** Builds on the parallel-batch loading from [#407](https://github.com/ResetShop/angular-nx-standalone-starter/issues/407). Each planning agent's Step 0 now splits into a **core** set loaded every time — `clean-architecture`, `solid`, `cupid`, `guiding-principles`, `cross-reference`, and the **hard-pinned** `coding-agent-policies` (plus `CLAUDE.md` for `plan-writer`) — and a **uniform domain** set — `auth`, `backend-api`, `domain-model`, `generators`, `accessibility` (the same for both planning agents) — gated by the branch diff via a glob→ref map (`src/api`/`src/db`/`src/contracts` → `backend-api` + `domain-model` + `auth`; guards/auth → `auth`; generator dirs → `generators`; `src/app/components`/templates/styles → `accessibility`). **Guardrails:** the agents **fail open** — empty, mixed-layer, or ambiguous diffs load every domain ref; `coding-agent-policies.md` is never gated; and **`code-reviewer` is deliberately excluded and continues to load its full set** (the review gate is the worst place for under-loading). `coding-agent-policies.md` is now explicitly part of the planning agents' always-load core **and** `code-reviewer`'s full-load set (previously enforced via the CLAUDE.md preamble but not listed in any agent's Step 0; `code-reviewer` now loads 12 references). The glob→ref map is documented in CLAUDE.md → "Conditional Reference Loading (planning agents)" with an explicit "when adding a new reference, update this map" staleness note. Agent-config-only; no runtime, schema, or fork-API impact. ([#410](https://github.com/ResetShop/angular-nx-standalone-starter/issues/410))
- **Reference-loading agents now load their `.claude/references/*` set in a single parallel batch.** Step-0 of `code-reviewer` (11 refs), `plan-writer` (9 refs + `CLAUDE.md`), `architecture-advisor` (8), `refactoring-specialist` (3), and `security-auditor` (2) previously listed reads as a numbered/serial sequence, which nudged one-at-a-time round-trips. The instructions now direct each agent to issue every `Read` call together in one message (parallel batch). **The full reference set is unchanged for every agent** — only the access pattern changes (D-parallel); `code-reviewer` continues to load all 11 references with an explicit note that conditional/diff-scoped loading is out of scope for it. Single-reference agents (`domain-model-advisor`, `test-generator`) are untouched. Agent-config-only; no runtime, schema, or fork-API impact. ([#407](https://github.com/ResetShop/angular-nx-standalone-starter/issues/407))
- **Decouple user-management from authentication-table writes (internal backend refactor).** `DrizzleUserManagementRepository` (`apps/reference-app/src/api/modules/user/user-management.repository.ts`) no longer writes to the `authentication` table: the `tx.insert(authentication)` inside `create()` and the entire `updatePasswordAndMustChange()` method are gone, and the `@schema/authentication` import is removed. Those writes move to the auth domain — `DrizzleAuthenticationRepository` (`apps/reference-app/src/api/modules/auth/authentication.repository.ts`) gains `createInitialPassword({ userId, passwordHash, mustChangePassword }, tx?)` (inserts the initial credential row) and `setPassword(userId, passwordHash, mustChangePassword, tx?)` (replaces the hash + flag, skips soft-deleted users via a joined `user`-table check, returns `false` when the user is missing or deleted), both added to the `AuthenticationRepository` interface and the `InMemoryAuthenticationRepository` mock. Cross-domain composition uses a new `BaseRepository.runInTransaction<T>(fn)` helper (`apps/reference-app/src/api/helpers/base.repository.ts`) plus a `DrizzleTransaction` type derived from the connector (`drizzle-postgres-connector.ts`): `UserManagementService.createUser` now opens a single transaction via `userManagementRepository.runInTransaction(...)` and threads the `tx` into `userManagementRepository.create({ email, firstName, lastName }, tx)`, `authenticationRepository.createInitialPassword(..., tx)`, and `userRoleRepository.replaceUserRoles(userId, roleIds, actorId, tx)` — role assignment (authorization) is likewise no longer written by the identity repository, so a failure in any of the three rolls back the user insert; `UserManagementService.resetPassword` now calls `authenticationRepository.setPassword(id, hash, true)`. The service gains `authRepository` and `userRoleRepository` constructor dependencies, resolved by Awilix from their existing cradle keys (no `container.ts` registration change). At the repo layer `create()` drops both its password fields and its role writes: `CreateUserWithHashedPasswordParams` is removed from `user-management/interfaces.ts` and replaced by the identity-only `CreateUserIdentityParams` (`{ email, firstName, lastName }` — no `roleIds`); `create(params, tx?)` (no `actorId`) and `runInTransaction` are added to the `UserManagementRepository` interface, and `UserRoleRepository.replaceUserRoles` gains an optional `tx` so it can join the creation transaction. **Request/response shapes, URLs, and frontend code are unchanged.** One status-code change: `POST /api/user` with non-existent `roleIds` now returns a documented `400` (`Roles not found`) — the same validation the `PUT /api/user/{id}/roles` endpoint already enforced — instead of the previous unhandled foreign-key error; the `201` success path and `POST /api/user/{id}/reset-password` are unchanged. **Migration:** forks that hand-instantiate `new UserManagementService({ … })` outside the container must add an `authRepository: AuthenticationRepository` constructor dep; forks that called `UserManagementRepository.updatePasswordAndMustChange(...)` or built `CreateUserWithHashedPasswordParams` directly must switch to `AuthenticationRepository.setPassword(...)` / `createInitialPassword(...)` and the identity-only `CreateUserIdentityParams` (`{ email, firstName, lastName }`); forks that hand-instantiate `new UserManagementService({ … })` must also add a `userRoleRepository: UserRoleRepository` constructor dep. ([#387](https://github.com/ResetShop/angular-nx-standalone-starter/issues/387))
- **`code-reviewer` agent verifies via cache-aware `npm run ci:verify` (decision B2).** `.claude/agents/code-reviewer.md` now points its "Review Process" step 4 and "Verification Results" section at `npm run ci:verify` instead of the cold `npm run ci`. The reviewer still runs the verification itself on **every** invocation — including the standalone Bug-fix (`implement → test-generator → code-reviewer`) and Refactoring (`refactoring-specialist → test-generator → code-reviewer`) pipelines that have no preceding orchestrator CI run — so its independence and standalone composability are preserved and it never reports a CI status it did not observe (this is decision **B2**; the rejected **B1** would have had the reviewer trust a handed-in result and skip verification in those pipelines). Because `ci:verify` rides the Nx cache, the second back-to-back CI run in a `/issue-workflow` pass (orchestrator's Phase 4 cold `ci`, then the reviewer) collapses from a cold rebuild to a near-instant cache restore on an unchanged tree. The cold `npm run ci` remains the orchestrator's authoritative final gate before the PR. Agent-config-only change; no runtime, schema, or fork-API impact. Depends on the `ci:verify` script added in [#404](https://github.com/ResetShop/angular-nx-standalone-starter/issues/404). ([#405](https://github.com/ResetShop/angular-nx-standalone-starter/issues/405))
- **`hashPassword` injected into `UserManagementService`; `getBcryptSaltRounds` made file-private.** New `apps/reference-app/src/api/services/password/password-hasher.ts` exports `createPasswordHasher()` — a named factory returning `(plain: string) => Promise<string>` backed by `bcryptjs.hash`. The closure resolves the salt rounds on every invocation (not at factory-creation time), so the integration harness can still lower `BCRYPT_COST` before the first hash runs. `getBcryptSaltRounds()` moved into that module as a **file-private** function and is no longer exported from `apps/reference-app/src/api/constants/auth.constants.ts`. `UserManagementService` (`src/api/modules/user/user-management.service.ts`) no longer imports `bcryptjs` or `getBcryptSaltRounds`; it accepts a `hashPassword` dependency on its constructor deps (mirroring the existing `generatePassword` injection) and both `createUser` and `resetPassword` call `this.hashPassword(plainPassword)`. The Awilix container registers `hashPassword: asValue(createPasswordHasher())` in `registerValues()` (`src/api/container/container.ts`), with the matching `Cradle` entry and dependency-graph comment in `container.types.ts`. `user-management.service.spec.ts` now injects a deterministic `mockHashPassword = fn<[string], Promise<string>>()` and asserts the plaintext flows into the hasher and the hash flows on to the repository — the unit suite no longer depends on `BCRYPT_COST` or real bcrypt. Integration tests are unchanged: `env-helpers.ts:configureEnvVars()` still sets `BCRYPT_COST=1` before container construction, which the hasher reads at call time. **Fork-visible note:** the change is additive at the container level — apps resolving `UserManagementService` from the container need no changes. **Migration:** forks that hand-instantiate `new UserManagementService({ … })` outside the container (or otherwise call `getBcryptSaltRounds` from `auth.constants.ts`) must add `hashPassword: createPasswordHasher()` to the constructor deps and import the helper from its new home. ([#385](https://github.com/ResetShop/angular-nx-standalone-starter/issues/385))
- **[hono-core] Split `environment.ts`: `isServerless()` moves to `runtime.ts`, `EnvironmentConfig` to `environment.types.ts`, `environment` constant moves per-app.** `packages/hono-core/src/lib/environment.ts` mixed framework infrastructure (`isServerless()`, `EnvironmentConfig`) with app-specific configuration (the `environment` constant reading `BASE_HREF`, `PG_CONNECTION_STRING`). The file is replaced by two package-owned files — `runtime.ts` (exports `isServerless()`) and `environment.types.ts` (exports `EnvironmentConfig`) — and a new app-owned `apps/reference-app/src/api/environment.ts`. The `basePath: process.env['BASE_HREF']` type unsoundness (`string | undefined` assigned to `string`) is fixed with `as string` and a `// REASON:` comment, consistent with the existing `PG_CONNECTION_STRING` coercion pattern in the same constant. The package root (`@resetshop/hono-core`) continues to re-export `isServerless` and `type EnvironmentConfig` — forks importing those two symbols see no change. First spec added to `packages/hono-core` (`runtime.spec.ts`, 3 cases) alongside new test infrastructure (`vitest.config.ts`, `tsconfig.spec.json`, `project.json` `test` target) mirroring the shape established in `packages/util/`. Commented-out import paths in the package's three sample connector templates (`clarity-connector.ts`, `drizzle-mysql-connector.ts`, `sanity-connector.ts`) updated from `./environment` to `../environment` so a fork user uncommenting them resolves to the new app-side file location. **Required migration for forks that imported `environment` from `@resetshop/hono-core`:** (1) Create `apps/<slug>/src/api/environment.ts` using `apps/reference-app/src/api/environment.ts` as a template. (2) Replace every `import { environment } from '@resetshop/hono-core'` with the appropriate relative path to that new file (e.g., `import { environment } from '../environment'` from `src/api/helpers/`, `import { environment } from '../api/environment'` from `src/db/`). (3) The repo-root `drizzle.config.ts` no longer imports from `./packages/hono-core/src/lib/environment` — it now reads `process.env['PG_CONNECTION_STRING']` directly. `drizzle.config.ts` is a workspace-root build-tool config that lives outside any Nx project, so importing from an Nx project there violates `@nx/enforce-module-boundaries`; it also only needs one env var, not the full app environment object. Forks that customised this file should drop the project-relative import and read `process.env` directly (run `grep -rn "./packages/hono-core/src/lib/environment" --include="*.ts" --include="*.mjs" --include="*.js"` from the repo root to find any remaining stale references). `isServerless` and `EnvironmentConfig` imports from `@resetshop/hono-core` require no changes. ([#258](https://github.com/ResetShop/angular-nx-standalone-starter/issues/258))
- **Align Bruno error-case docs to the `auth: none` cookie convention.** Five `.bru` files under `docs/api/user/` — `Create User - Duplicate Email.bru`, `Create User - Invalid Roles.bru`, `Delete User - Not Found.bru`, `Get User - Not Found.bru`, `Update User - Not Found.bru` — previously declared `auth: bearer` and carried a dead `auth:bearer { token: {{accessToken}} }` block. The project uses HttpOnly cookie authentication; Bruno's `auth` field only sets the `Authorization` header, which the API never reads, so these blocks were no-ops at the HTTP level. All five files now use `auth: none` (matching the 61 already-aligned files in the collection), and the dead token blocks are removed. Verified by `grep -rl "auth: bearer" docs/api --include="*.bru"` returning no results. No HTTP-level behavior change — the requests were already executing without the `Authorization` header. Forks consuming the Bruno collection inherit the cleanup on merge; no migration required. ([#384](https://github.com/ResetShop/angular-nx-standalone-starter/issues/384))
- **Fix stale code examples in `.claude/references/domain-model.md` tactical sections.** Two snippets had drifted from the real source files. (1) The "Factory Functions" example listed a `token: string` field on `CreateUserOptions` and passed it as a sixth argument to `new User(...)`; the real `CreateUserOptions` in `apps/reference-app/src/app/domain/user/user.mapper.ts` has no `token` field and the `User` constructor takes exactly five arguments — both removed from the snippet. (2) The "Performance: O(1) Lookups" example showed a two-argument `hasPermission(resource: string, action: string)` with a backtick-concatenated lookup; the real `User.hasPermission` (in `user.model.ts`) and the `IUser`/`IRole` interfaces use a single-argument `hasPermission(identifier: string)` form — snippet updated to match, also resolving an internal inconsistency with the "Aggregate Design" strategic section added in #207 which already used the single-arg form. Agent-facing reference only: no runtime code, schema, contract, or behavior change. Forks consuming `.claude/references/*` inherit the corrected snippets on merge; no migration required. ([#380](https://github.com/ResetShop/angular-nx-standalone-starter/issues/380))
- **`CurrentUser` service + `CURRENT_USER_SOURCE` token in `@resetshop/angular-core` — plus delete-row self-hide in users-list.** (a) New `packages/angular-core/src/lib/auth/` sub-module exports two primitives for cross-app reuse: a `CurrentUserSource` interface + `CURRENT_USER_SOURCE` injection token (`current-user.token.ts`) as the DIP inversion seam, and a `CurrentUser` injectable service (`current-user.ts`, `providedIn: 'root'`) exposing `is(entity)` — a reactive `(entity: { id: number } | null | undefined) => boolean` check resolved against the wired source. Both live behind `@resetshop/angular-core/auth/*` path aliases. The reference app wires `AuthStore` as the `CURRENT_USER_SOURCE` implementation via an unconditional `{ provide: CURRENT_USER_SOURCE, useExisting: AuthStore }` entry in `provideAuth()` — `IUser` has `readonly id: number`, so `Signal<IUser | null>` is structurally assignable to `Signal<{ id: number } | null>` (signal read-only covariance), no AuthStore changes required. Coverage: 6 unit tests for `CurrentUser` in `packages/angular-core` (id match, id mismatch, null current user, null entity, undefined entity, reactivity through a `computed`). (b) Refactors the existing `canResetPassword` guards in `users-list.ts` and `user-card.ts` to call `currentUser.is(row)` directly inside `@if (!currentUser.is(row)) { … }` blocks (per-component `canResetPassword` wrappers removed; `user-card.ts` no longer injects `AuthStore` directly), then adds symmetric delete-self gating: the Delete row action in the users-list table and the mobile `user-card` is now hidden when the row's user id matches the authenticated user's id. This prevents the misleading destructive-flow confirmation dialog for an operation the backend was always going to reject with `SELF_LOCKOUT` → 403. Edit remains visible on the self-row — users may legitimately update their own profile. The existing reset-password self-hide tests in `users-list.spec.ts` and `user-card.spec.ts` were widened (description and assertions) to also cover the delete-self-hide case in the same render. **Fork-visible note:** the new token + service in `packages/angular-core` are additive — existing apps that do not inject `CurrentUser` are unaffected. The `provideAuth()` change propagates to forks via upstream merge; forks that have customised `provideAuth()` need to add the `{ provide: CURRENT_USER_SOURCE, useExisting: AuthStore }` binding to keep `CurrentUser` resolving — no migration required for forks that have not customised `provideAuth()`. ([#389](https://github.com/ResetShop/angular-nx-standalone-starter/issues/389))
- **Modal-stack mobile sizing.** Four overlay components now size correctly on narrow viewports. (6) Toast width capped at `w-[min(350px,calc(100vw-2rem))]` — leaves 1 rem visual margin per side on sub-382 px screens instead of pinning to a fixed 350 px. The class moves from `:host` `@apply` in `toast-notification.css` to the `hostClasses` computed binding in `toast-notification.ts` so it's discoverable on the rendered host element. A separate `:host[data-position-x='center']` CSS rule (`left: 50%; margin-left: calc(min(350px, 100vw - 2rem) / -2);`) horizontally centres the toast on the zero-width fixed container that `NgpToastManager` provides for `*-center` placements — previously absent, so the toast anchored to `left: 0` of that container and overflowed the right edge by its full width on narrow viewports. (8) Four CRUD drawers (`create-user-drawer`, `edit-user-drawer`, `create-role-drawer`, `edit-role-drawer`) changed from `class="lg:w-lg"` to `class="w-full sm:w-lg"` — the 512 px panel now engages from approximately the `sm:` breakpoint + 43 px (where 75 % of viewport first exceeds 512 px and `sm:w-lg` wins over the base `sm:max-w-3/4` cap) instead of from the `lg:` breakpoint. The base `DrawerPanel` `w-screen sm:max-w-3/4` is unchanged. (12) Confirm dialog now has its width constraint on the `<dialog>` itself rather than the inner content div, via `w-full max-w-[min(calc(100vw-2rem),28rem)]` — guarantees a 1 rem gutter on each side at every viewport ≤ 28 rem (448 px) and caps at 28 rem from then on. The inner div is reduced to `p-4 sm:p-6` (padding only). The original inner-div-based approach failed because `<dialog>` opened with `showModal()` is `fit-content` by default, so `w-[calc(100%-2rem)]` on the inner div resolved against the dialog and created a circular dependency that let the dialog go edge-to-edge on viewports below ~360 px. A `data-testid="confirm-dialog-content"` is retained on the inner div for regression-guard queries. (14) Select dropdown `max-h-60` (240 px fixed) moves to a literal `class="max-h-[min(60vh,240px)]"` on the `*ngpSelectPortal` div — caps the dropdown at 60 % of the viewport on short screens (60vh ≈ 225 px on a 375 px-tall device), 240 px otherwise. The class moves from `[ngpSelectDropdown]` CSS `@apply` to a template class so it's testable. `data-testid="select-dropdown"` added on the same div. New regression-guard unit tests for items 8, 12, 14; new `MobileViewport` Storybook stories for all four items. **No consumer changes required.** No API, store, schema, or translation-key changes. ([#253](https://github.com/ResetShop/angular-nx-standalone-starter/issues/253) → sub-issue [#344](https://github.com/ResetShop/angular-nx-standalone-starter/issues/344))
- **Password wordlists embedded at build time; removed runtime filesystem read.** `apps/reference-app/src/api/utils/password.ts` previously loaded `en-password-seed.txt` and `es-password-seed.txt` at runtime via `readFile`, trying two candidate paths (one anchored to `import.meta.dirname`, one to `process.cwd()`). Both failed in Vite dev: `import.meta.dirname` resolved to `.angular/vite-root/reference-app/` (HMR-rooted virtual location), and `process.cwd()` resolved to the workspace root — neither candidate found the source `apps/reference-app/src/api/utils/wordlists/` directory, so `generatePassword` threw "Word list file not found" on every dev invocation. The wordlists are now imported with the `?raw` query suffix (Vite and esbuild both inline the file content at build time), parsed once at module evaluation, and stored in a frozen `WORDLISTS` dictionary. A new `wordlists.d.ts` declares the `*.txt?raw` module shape for TypeScript. The previously-orphan `copy-server-assets` Nx target in `apps/reference-app/project.json` (which referenced a `scripts/copy-server-assets.mjs` that never existed) is removed since runtime asset copying is no longer needed. The dictionary lookup uses `Object.hasOwn(WORDLISTS, language)` instead of `WORDLISTS[language]`, closing a residual `'__proto__'` / `'constructor'` access surface and replacing the prior `// TODO (#159): Validate language against an allowlist to prevent path traversal` comment with a working allowlist check. `generatePassword`'s `Promise<string>` signature is preserved for downstream caller compatibility (`container.types.ts`, `user-management.service.ts`); only the internal implementation became synchronous. **No fork-visible API change** — \`generatePassword\` callers see no difference. ([#371](https://github.com/ResetShop/angular-nx-standalone-starter/pull/371))
- **Page-level layout chrome responsive fixes.** Seven defects from the responsiveness epic addressed in one sub-issue. (5/7) Users/Roles list page search field and Create button now stack vertically below the `sm:` breakpoint (`flex-col items-stretch gap-3 sm:flex-row sm:items-center sm:justify-between sm:gap-4`) and sit side-by-side from `sm:` up; Create button is full-width on mobile (`w-full sm:w-auto`); the skeleton placeholder for the button is also full-width on mobile. (19) Header host horizontal padding reduced from `px-4` to `px-2 sm:px-4` — gives ~16 px extra usable width on 360–375 px screens. (9) Breadcrumb hides intermediate segments below `sm:`, showing first item + `…` ellipsis + last item via a new `entries` computed signal in `apps/reference-app/src/app/components/breadcrumb/breadcrumb.ts`. The signal emits each rendered position as an entry with a `viewport: 'all' | 'mobile' | 'desktop'` flag; the wrapping `<li>` applies `hidden`/`sm:inline-flex`/`sm:hidden` based on that flag. Intermediate items stay in the DOM and are visually hidden on mobile via `display: none` (`hidden` / `sm:inline-flex`); the screen reader trail on mobile reflects the abbreviated `first › last` chain. Full chain restores from `sm:` up. (10) Dashboard `<main>` padding reduced from `p-4` to `p-2 sm:p-4`. (11) Page-shell title scales from `text-2xl` to `text-xl sm:text-2xl`; both `space-y-6` section gaps tighten to `space-y-4 sm:space-y-6`. (17) Card padding steps from `p-4` to `p-3 sm:p-4 md:p-5`. New `MobileEllipsis` (`Breadcrumb`), `MobileViewport` (`Header`, `Card`), and `MobileLayout` (`PageShell`) Storybook stories at the 375 px viewport preset. Four new `breadcrumb.spec.ts` cases (single-item, two-item, 3+-item ellipsis class, 4-item intermediate hidden class) and one new `page-shell.spec.ts` case (responsive heading classes). No API, store, or translation-key changes. ([#253](https://github.com/ResetShop/angular-nx-standalone-starter/issues/253) → sub-issue [#343](https://github.com/ResetShop/angular-nx-standalone-starter/issues/343))
- **Pagination mobile layout + native select font size.** `<app-pagination>` (`packages/ui/src/lib/pagination/pagination.ts`) now stacks vertically below the `sm:` breakpoint (`flex-col items-stretch gap-3 sm:flex-row sm:items-center sm:justify-between sm:gap-4`) so the rows-per-page selector sits on top of the page-navigation row instead of competing for horizontal space. Individual page-number buttons and ellipsis spans gain `hidden sm:inline-flex` / `hidden sm:flex` — they are removed from mobile layout and replaced by a translated `"Page N of M"` indicator (`sm:hidden`, `aria-live="polite"`, `aria-atomic="true"`) that sits between the always-visible prev and next buttons. Prev and next buttons gain `data-touch-target` so their hit area extends to 44 px on mobile while the visual `h-8 w-8` (32 px) stays unchanged. The native `<select>` already carries `text-base sm:text-sm` (the iOS Safari zoom-on-focus fix from sub-issue A) — a regression-guard test ships alongside this change. New `PAGINATION.PAGE_OF` translation key (`"Page {current} of {total}"` / `"Página {current} de {total}"`) added to `translations.schema.ts`, `en.ts`, `es.ts`, the app-level `translation.mock.ts`, and the `translation.spec.ts` schema fixtures. Seven new `pagination.spec.ts` cases cover the wrapper stacking classes, hidden page buttons, mobile current-page label, aria-live wiring, prev/next touch targets, and select font-size regression. A new `Mobile` Storybook story renders at the 375 px viewport preset for both `packages/ui` and `apps/reference-app` mirror story files. **No consumer changes required** — `users-list`, `roles-list`, and `permissions-list` rely on the same `<app-pagination>` API with no new inputs. ([#253](https://github.com/ResetShop/angular-nx-standalone-starter/issues/253) → sub-issue [#342](https://github.com/ResetShop/angular-nx-standalone-starter/issues/342))
- **DataTable iteration 2 — built-in viewport detection, display-mode toggle, grouped card rendering, per-list card components.** Follow-up refinement to the iteration-1 `DataTable` work; both ship together. Four orthogonal architectural improvements: (1) **Viewport detection moves into DataTable.** New `cardsBelow = input<'sm' | 'md' | 'lg' | null>(null)` automatically switches `<app-data-table>` to card mode below the given Tailwind breakpoint. Consumers drop their duplicated `createSmViewportSignal()` factory entirely. A new reusable `createBreakpointSignal()` utility lives in `@resetshop/angular-core/breakpoint` (`packages/angular-core/src/lib/breakpoint/breakpoint.ts`, with 7 unit tests) so future consumers can call it directly without reinventing the CSS-custom-property lookup. **`@angular/cdk` is now a peer dependency of `@resetshop/angular-core`.** (2) **Display-mode toggle.** New `displayModes = input<Array<'table' | 'cards'>>(['table'])` input — when the array has more than one entry and a card template is projected, DataTable renders a small `featherTable`/`featherGrid` toggle button group in the top-right corner with `role="group"` semantics, `aria-pressed` state, and `data-touch-target` for mobile hit-area compliance. Two-way `[(displayMode)]` binding via the new `displayModeChange` output. Precedence: explicit `[displayMode]` > user click > `cardsBelow` responsive default. (3) **Grouped card rendering.** `[grouping]` rows now render as non-interactive `<li role="group" aria-label="...">` section headers (matching PrimeNG `p-dataView` grouped and iOS grouped `UITableView` patterns) instead of being flattened. Card mode forces TanStack `expanded: true` so all sub-rows are always visible — collapse remains a table-mode-only interaction. (4) **Per-list card components.** The three reference-app list pages each lost their inline `<ng-template appDataTableCardDef>` markup, replaced by new page-scoped `UserCard`, `RoleCard`, and `PermissionCard` components (`apps/reference-app/src/app/pages/dashboard/{users,roles,permissions}/<list>/<x>-card.ts`) with their own specs and Storybook stories. Net line-count reduction: ~70 lines per list page. Three new `DATA_TABLE.TOGGLE.{TABLE,CARDS,GROUP_LABEL}` translation keys added to `translations.schema.ts`, `en.ts`, `es.ts`, and the test mock. 12 new `data-table.spec.ts` test cases (toggle, `cardsBelow`, grouped cards) plus 18 new card-component test cases. Three new Storybook stories: `WithToggleControl`, `GroupedCards`, and per-card-component stories. **Forks consuming the iteration-1 DataTable API gain new optional inputs — no breaking changes.** ([#253](https://github.com/ResetShop/angular-nx-standalone-starter/issues/253) → sub-issue [#341](https://github.com/ResetShop/angular-nx-standalone-starter/issues/341))
- **DataTable card display mode + responsive cell wrapping + icon-first row action buttons.** Three orthogonal data-table responsiveness improvements landed together. (1) **Card display mode** — the shared `<app-data-table>` (`packages/ui/src/lib/data-table/data-table.ts`) gained a `displayMode` input (`'table' | 'cards'`, default `'table'`) and a new `DataTableCardDef` directive (`packages/ui/src/lib/data-table/data-table-card-def.ts`, exported from `@resetshop/ui`). When `displayMode="cards"` and a consumer projects an `<ng-template appDataTableCardDef let-row>` template, the component renders a vertical `<ul role="list">` stack of cards (preserving TanStack Table's sort order and row model) instead of a `<table>`. The new `resolvedDisplayMode` computed signal falls back to `'table'` rendering whenever the card template is absent — a safe default, so consumers can opt in incrementally. Grouped rows are intentionally flattened in card mode (group headers are a table-mode feature). Loading and empty states work in both modes. (2) **Cell whitespace fix** — the default cell `<td>` class in `packages/ui/src/lib/data-table/data-table.html` lost its hard-coded `whitespace-nowrap`, so cells with long content (emails, descriptions) now wrap naturally on tablet viewports instead of forcing horizontal scroll. Columns that need single-line rendering can opt back in via column-specific cell classes. (3) **Row action buttons** — Edit/Delete buttons in `apps/reference-app/src/app/pages/dashboard/users/users-list/users-list.ts` and `roles-list.ts` switched from text-only to icon-first (`featherEdit3`, `featherTrash2` from `@ng-icons/feather-icons`), with the translated label hidden below `sm:` via `sr-only sm:not-sr-only` (kept in the accessibility tree at all viewports). Both buttons gained `data-touch-target` to enforce a 44px hit area on mobile. The three reference-app list pages (`users-list`, `roles-list`, `permissions-list`) each adopted the card mode at viewports below `sm:` via a local `createSmViewportSignal()` factory replicating `Sidebar.createLgViewportSignal()` from sub-issue C (`BreakpointObserver` reading Tailwind 4's `--breakpoint-sm` CSS custom property with a `40rem` fallback). Five new `data-table.spec.ts` test cases cover card rendering, table fallback, loading/empty states, and sort-order preservation in card mode; six new consumer-spec cases (`users-list.spec.ts`, `roles-list.spec.ts`) cover the responsive desktop/mobile switch. Three new Storybook stories: `PlaygroundWithCardToggle` (`displayMode` control), `CardView` (consumer-supplied two-column card template), and `CardViewCompact` (mobile viewport preset). ([#253](https://github.com/ResetShop/angular-nx-standalone-starter/issues/253) → sub-issue [#341](https://github.com/ResetShop/angular-nx-standalone-starter/issues/341))
- **Backfilled `parameters.docs.canvas.sourceState: 'shown'` across 17 previously non-compliant `*.stories.ts` files** so the new `custom-storybook/storybook-source-state` ESLint rule (see Added) passes from the moment it is registered. Affects 7 files in `packages/ui/src/lib/` (`combobox`, `confirm-dialog`, `data-table`, `drawer`, `form-field`, `pagination`, `select`), 8 files in `apps/reference-app/src/app/components/` (same component set plus `app.stories.ts` at the app root), and 2 page-level stories (`pages/auth/login/login.stories.ts`, `pages/auth/reset-password/reset-password.stories.ts`). Files that previously had a `parameters.docs` block gain a sibling `canvas: { sourceState: 'shown' }` entry; files with no `parameters` block at all gain the full `parameters: { docs: { canvas: { sourceState: 'shown' } } }` shape. No story args, decorators, render functions, or test play scripts were touched — the diffs are 3–8 lines per file. ([#348](https://github.com/ResetShop/angular-nx-standalone-starter/issues/348))
- **Auth pages adopt a fullscreen-on-mobile, card-on-desktop layout** via a new `<app-immersive-panel>` component (`packages/ui/src/lib/immersive-panel/immersive-panel.ts`) — **no changes to `<app-card>`**. The new component has the same template-input API as `<app-card>` (`titleTemplate`, `subtitleTemplate`, `contentTemplate`, `footerTemplate`) but applies card chrome (`rounded-xl border shadow-2xs`) only from the `sm:` breakpoint up. Below `sm:` the panel renders full-height and chromeless, making the auth pages occupy the entire viewport with no border, no rounded corners, no shadow, and no surrounding backdrop. Login and reset-password swap `<app-card>` for `<app-immersive-panel>` and gain full responsive parity: both pages use `h-svh w-svw sm:h-[420px] sm:w-[420px]` form sizing, `w-full max-w-96` content width (fixes the `w-96` overflow on viewports < 384 px), and conditional `:host` backdrop (`bg-card` on mobile, `bg-black/95` from `sm:` up). New Storybook stories for both `ImmersivePanel` and the reset-password page; both auth-page stories default to the `Mobile (375px)` viewport preset. ([#253](https://github.com/ResetShop/angular-nx-standalone-starter/issues/253) → sub-issue [#339](https://github.com/ResetShop/angular-nx-standalone-starter/issues/339))
- **Mobile form interaction quality** — form inputs now use `text-base` (16px) below the `sm:` breakpoint and `text-sm` (14px) from `sm:` up. This prevents the iOS Safari zoom-on-focus regression that was hitting every form in the project (the WebKit auto-zoom triggers on inputs with font-size below 16px). Affects `packages/ui/src/lib/form-field/form-field.ts` (input/select/textarea via `::ng-deep`), `packages/ui/src/lib/select/select.css` (`[ngpSelect]` trigger), `packages/ui/src/lib/combobox/combobox.css` (`[ngpComboboxInput]`), and the inline `<select>` in `packages/ui/src/lib/pagination/pagination.ts`. New `[data-touch-target]` accessibility utility added globally (via the new `packages/ui/src/theme/accessibility.css` imported from `tailwind.config.css`) — opt-in attribute that extends an element's hit area to ≥44px (WCAG 2.5.5 / Apple HIG minimum) by injecting a `::before` overlay with `-inset-3` extension, without changing visual size. Applied to permission/role selector `<label>` rows in `apps/reference-app/src/app/pages/dashboard/{users/role-selector,roles/permission-selector}/`. Selector rows also restructured: name and description now stack vertically on mobile, side-by-side from `sm:` up, with the em-dash separator hidden on mobile where vertical stacking already provides visual separation. The permission-selector resource header was converted from a `<div>` to a `<label>` so the entire header row is clickable, not just the checkbox. Storybook `.storybook/preview.ts` gains viewport presets (Mobile 375px, Tablet 768px, Desktop 1280px) — reviewers can switch viewports interactively for every story. ([#253](https://github.com/ResetShop/angular-nx-standalone-starter/issues/253) → sub-issue [#338](https://github.com/ResetShop/angular-nx-standalone-starter/issues/338))
- **Generators are now documented for both humans and agents.** Added a new "Generators" section to `README.md` (Project Setup Guide) that enumerates all seven generators with their `nx g @resetshop/generators:<name>` invocation and a one-line "use when" description, plus disambiguation guidance for choosing between `crud` and the narrower single-layer generators. Added a new agent-facing reference at `.claude/references/generators.md` covering each generator's exact output, naming conventions (the `names()` helper produces kebab-case file paths, camelCase JS identifiers, PascalCase types from any input form), known limitations (page generator's `../../providers` and `../../store` walk-up only resolves correctly from the default `src/app/pages/dashboard` directory; drizzle-schema's `pgTable('orderLineItem', ...)` string differs from the snake_case convention used in existing schemas), and links to each generator's executable spec. Wired the new reference into the Step 0 load list of `architecture-advisor`, `plan-writer`, and `code-reviewer` so agents see it during planning and review. Replaced the "as they get adopted" weasel-wording in CLAUDE.md's Nx Generators subsection with a list of every generator and a pointer to the deeper reference. ([#333](https://github.com/ResetShop/angular-nx-standalone-starter/issues/333))
- **Local integration tests now run zero-config via `embedded-postgres`.** When `PG_TEST_CONNECTION_STRING` is unset, `apps/reference-app/src/api/integration/setup/global-setup.ts` spawns a real Postgres 17 cluster on a free localhost port via the [`embedded-postgres`](https://www.npmjs.com/package/embedded-postgres) package (official EnterpriseDB binaries shipped by the package, downloaded once at `npm install` time and cached in `node_modules`), pushes the schema, seeds base data, runs the suite, and tears the cluster down on teardown. CI behavior is unchanged — it still sets `PG_TEST_CONNECTION_STRING` to its `postgres:17` service container, so the embedded path is skipped. New file `apps/reference-app/src/api/integration/setup/embedded-pg-test-db.ts` owns the cluster lifecycle (free-port allocation, init + start + stop). The existing `drizzle-postgres-connector.ts`, `db-helpers.ts`, `env-helpers.ts`, and `integration-setup.ts` are untouched. `scripts/skip-if-no-db.mjs` removed and the `apps/reference-app/project.json` `test-integration` target dropped its skip-prefix — the gate is meaningless now that local runs always have a DB. ([#321](https://github.com/ResetShop/angular-nx-standalone-starter/issues/321))
- **`embedded-postgres` devDependency** added — ships official EnterpriseDB Postgres 17 binaries used to spawn a real Postgres cluster locally for integration tests with no Docker daemon required. ([#321](https://github.com/ResetShop/angular-nx-standalone-starter/issues/321))
- **`apps/reference-app/src/app/guards/permission.guard.ts`** — emits a translated toast (`PERMISSIONS.ERRORS.ACCESS_DENIED`) via `UIStore.showNotification` when denying access, instead of silently redirecting to `/dashboard`. Mirrors the `forbidden.interceptor.ts` 403 toast pattern so users get consistent feedback whether the deny happens at the route layer or the HTTP layer. The redirect target `/dashboard` is gated by `authGuard`, not `permissionGuard`, so the toast fires exactly once per deny — verified by `permission.guard.spec.ts`. New translation key `PERMISSIONS.ERRORS.ACCESS_DENIED` added to `translations.schema.ts`, `en.ts`, and `es.ts`. ([#323](https://github.com/ResetShop/angular-nx-standalone-starter/issues/323))
- **`apps/reference-app/src/app/pages/dashboard/pages/dashboard-home/dashboard-home.ts`** — renders an explicit "No module access" alert at the top of the page when the current user holds zero permissions (`AuthStore.userPermissions().length === 0`). Previously such users saw only the unguarded Welcome / Settings / Health cards with no explanation of why the admin modules weren't visible; the alert now tells them to contact their administrator. The alert is additive — the unguarded cards still render below it. New translation keys `DASHBOARD.HOME.NO_ACCESS_TITLE` and `DASHBOARD.HOME.NO_ACCESS_MESSAGE` added to `translations.schema.ts`, `en.ts`, and `es.ts`. New `dashboard-home.spec.ts` covers both the visible and hidden cases. ([#323](https://github.com/ResetShop/angular-nx-standalone-starter/issues/323))
- **`POST /api/auth/login` and `GET /api/auth/me` response shape** — login response now includes the user's full roles with their permissions, mirroring `/api/auth/me`. The shared `authUserSchema` (`apps/reference-app/src/contracts/user/user.schemas.ts`) gained a `roles: z.array(roleWithPermissionsSchema)` field; `meResponseSchema` (`apps/reference-app/src/contracts/auth/auth.schemas.ts`) is now `meResponseSchema = authUserSchema`, replacing a 5-field inline duplicate. Backend `AuthService.authenticate` injects `userRoleService` and populates roles before returning. Frontend `mapLoginResponseToUser` reads `response.user.roles`, eliminating the "empty-roles window" that previously existed between login and the first protected-route activation (the symptom: permission-guarded sidebar items missing on re-login after token expiry). `validateSession()` (called by `authGuard` on `/dashboard`) still runs and refreshes the user, but is no longer the sole source of role data after login. **Forks consuming `LoginResponse` or `MeResponse` types must update their expectations** — both now carry roles + permissions. ([#322](https://github.com/ResetShop/angular-nx-standalone-starter/issues/322))
- **`.github/workflows/ci.yml` `test-integration` job** — switched `PG_TEST_CONNECTION_STRING` and `INTEGRATION_TEST_ADMIN_PASSWORD` from `secrets.*` to `vars.*` (GitHub Actions repository Variables, not Secrets). Neither value is sensitive — the workflow already declares an ephemeral `postgres:17` service container in the same job, so the connection string just points at `localhost:5432` and the admin password is a CI-only literal that exists only inside the ephemeral container. **Migration:** in repo Settings → Secrets and variables → Actions → **Variables** tab, add `PG_TEST_CONNECTION_STRING = postgresql://postgres:postgres@localhost:5432/test_db` and `INTEGRATION_TEST_ADMIN_PASSWORD = <any test-only password>`. The old `secrets.*` entries with the same names can be deleted afterwards. ([#319](https://github.com/ResetShop/angular-nx-standalone-starter/issues/319))
- **Drawer responsiveness on mobile + dialog UA reset architecture.** Left/right drawers now take full viewport width below the `sm:` breakpoint, cap at 75% from `sm:` up via `sm:max-w-3/4`, and accept consumer-side breakpoint-prefixed width overrides (e.g. `class="lg:w-lg"`) that layer on top of the base cap rather than replacing it. Top/bottom drawers mirror this on the height axis. The four CRUD drawers under `apps/reference-app/src/app/pages/dashboard/{users,roles}/{create,edit}-*-drawer/*.ts` switched from `class="w-lg"` (unconditional 512px) to `class="lg:w-lg"` (only at `lg:` up), so they render edge-to-edge on phone, 75% on tablet, and 512px on desktop. To make the responsive cap actually engage, the project's CSS Reset (`apps/reference-app/src/app/assets/css/reset.css`) gained a new item 11 that resets `<dialog>` `max-width`/`max-height` to `none` — neutralising the user-agent `:-internal-dialog-in-top-layer` `calc(100% - 2em - 6px)` cap that was clipping every drawer to ~94% of the viewport regardless of author CSS. The rule is wrapped in `@layer base` so Tailwind utilities (in `@layer utilities`) can override it; an unlayered author rule would beat every layered utility regardless of specificity and silently defeat any author attempt to cap dialog widths or heights. Storybook `.storybook/preview.ts` gained a new `Mobile landscape (640px)` viewport preset at the `sm:` boundary so reviewers can isolate `sm:` deltas from `md:` deltas, and each preset now carries an inline comment naming the active variant set. ([#338](https://github.com/ResetShop/angular-nx-standalone-starter/issues/338))
- **Accessibility utility documentation extracted to an agent-loaded reference.** The long doc block that lived inside `packages/ui/src/theme/accessibility.css` (purpose, usage examples, `-inset-3` sizing math, sibling-overlap and overflow-clipping limitations, and the `pointer-events: none` rationale) moved to a new `.claude/references/accessibility.md`. The CSS file keeps a short pointer comment plus the rules themselves. The new reference also adds an explicit review checklist for agents (when to apply `data-touch-target`, when to flag invented hit-area extensions, etc.) and a `Future utilities` placeholder for additional a11y helpers. Wired into the Step 0 load list of `code-reviewer`, `plan-writer`, and `architecture-advisor`, and reflected in the `Agent Reference Loading` table in `CLAUDE.md` (now "All 11 references" for `code-reviewer`). ([#338](https://github.com/ResetShop/angular-nx-standalone-starter/issues/338))
- **Codebase-wide sweep for duration-string convention violations** (`apps/reference-app/src/api/cron-jobs.ts`, `apps/reference-app/src/app/store/users/users.store.ts`, `apps/reference-app/src/app/store/roles/roles.store.ts`, plus the four CRUD drawer components and their specs under `apps/reference-app/src/app/pages/dashboard/{users,roles}/{create,edit}-*-drawer/`). Removed the last `_MS`-suffixed duration constants (`MIN_INTERVAL_MS`, `MAX_INTERVAL_MS`, `DEFAULT_INTERVAL_MS`, `PARSED_ENV_INTERVAL_MS`) from `cron-jobs.ts` per the CLAUDE.md "Duration String Convention" — duration strings now live as local constants without the suffix and `parseDurationToMs()` is called at the point of use instead of stored in a resolved-value constant. Two new shared constants extract previously-inline duration-string literals reused across multiple files: `SEARCH_DEBOUNCE_DELAY = '300ms'` in `apps/reference-app/src/app/store/store.constants.ts` (consumed by `users.store.ts` and `roles.store.ts`), and `DRAWER_CLOSE_AFTER_SUCCESS_DELAY = '1s'` in `apps/reference-app/src/app/pages/dashboard/dashboard.constants.ts` (consumed by the four CRUD drawer components and their specs). Also extended `apps/reference-app/src/contracts/common/duration.schemas.ts`'s `durationStringSchema` regex from `^\d+[dhms]$` to `^\d+(ms|[dhms])$` so the `ms` unit is permitted at the type level (the runtime `parseDurationToMs` already accepts it). **`TOKEN_CLEANUP_INTERVAL_MS` env var renamed to `TOKEN_CLEANUP_INTERVAL` and now accepts a duration string** (e.g. `24h`, `1h`, `7d`) instead of a millisecond integer — bringing it in line with `PASETO_ACCESS_TOKEN_EXPIRY`, `PASETO_REFRESH_TOKEN_EXPIRY`, `AUTH_LOCKOUT_DURATION` and the rest of the auth env-var surface. **Migration:** rename `TOKEN_CLEANUP_INTERVAL_MS=86400000` to `TOKEN_CLEANUP_INTERVAL=24h` in `.env`. If the old name is left set after this upgrade, the cron job will silently fall back to the 24h default (the cleanup endpoint at `GET /api/auth/cleanup-tokens` remains available regardless and is unaffected). README and `docs/AUTHENTICATION.md` updated to document the new name and shape. ([#259](https://github.com/ResetShop/angular-nx-standalone-starter/issues/259))

### Removed

- **Placeholder `/dashboard/welcome` route and `Welcome` component.** The hardcoded setup-checklist page (carrying a `TODO: Remove…` since inception, and already out of scope for the #261 e2e epic) is removed: the `welcome` route in `apps/reference-app/src/app/pages/dashboard/dashboard.routes.ts`, the `Welcome` component directory (`pages/dashboard/pages/welcome/`), and the `welcome` navigation entry in `dashboard.navigation.ts` (its `home` section is kept — it still holds the **Settings** link — and the now-unused `featherHome` import is dropped). The orphaned translation keys `DASHBOARD.HOME.NAV` and `DASHBOARD.HOME.DESCRIPTIONS.WELCOME` are removed from `TranslationSchema` (`packages/angular-core/src/lib/i18n/translations.schema.ts`), `en.ts`, `es.ts`, and the `translation.spec.ts` fixtures, and the corresponding `welcome` entry is dropped from `dashboard-home.ts`'s description `keyMap`. Spec/mocks cleaned up: `sidebar.spec.ts` (welcome route + fixture entry + assertions), `dashboard.spec.ts` (welcome route + fixture entry + unused `featherHome`), and the unused `WelcomePageComponent` in `mocks/component.mocks.ts`. The welcome **email** feature is unrelated and untouched; the broader onboarding-guidance rethink is tracked separately for 1.1.0. **Migration:** forks that linked to `/dashboard/welcome`, imported the `Welcome` component, or referenced `DASHBOARD.HOME.NAV` / `DASHBOARD.HOME.DESCRIPTIONS.WELCOME` must remove those references (the keys' absence now fails the `TranslationSchema` type-check until dropped). ([#458](https://github.com/ResetShop/angular-nx-standalone-starter/issues/458))
- **Delete legacy env-handling: `validateEnvironment` and `EnvironmentConfig` — PR B4 of the [#354](https://github.com/ResetShop/angular-nx-standalone-starter/issues/354) env-contract stack.** `apps/reference-app/src/api/container/validate-environment.ts` and its spec are deleted, and the `validateEnvironment()` call is removed from `createAwilixContainer()` in `container.ts`. Its three hand-rolled checks (PASETO_SECRET_KEY format, PASETO_ISSUER presence, EMAIL_PROVIDER validity) are fully superseded by the Zod sub-schemas in `@config/*.env.ts`, which validate on first proxy access and `process.exit(1)` with a formatted FATAL message — `container.verify()` at server startup resolves every registration, triggering those reads, so config still fails fast at boot (no validation gap). `packages/hono-core/src/lib/environment.types.ts` (the `EnvironmentConfig` interface) and its `@resetshop/hono-core` re-export are deleted — no in-repo consumer remained after the B1–B3 migration. **Migration:** forks importing `EnvironmentConfig` from `@resetshop/hono-core` must switch to the relevant `<Domain>Env` type (`AuthEnv` from `@config/auth.env`, `HttpEnv` from `@config/http.env`, `DbEnv` from `@config/db.env`, etc.) or define their own typed config interface; forks that called `validateEnvironment()` directly should rely on the proxy/`container.verify()` fail-fast instead. ([#443](https://github.com/ResetShop/angular-nx-standalone-starter/issues/443))

### Fixed

- **`forbiddenInterceptor` 403 toast did not render on routes that never activate the toast bridge.** After the #471 toast refactor, `ToastBridgeService` (the renderer) is a `providedIn: 'root'` singleton that is **lazily** instantiated — it only comes alive once a route calls `provideToast()`. But `forbiddenInterceptor` (`apps/reference-app/src/app/interceptors/forbidden.interceptor.ts`) fires a 403 toast via `UIStore.showNotification()` on **any** page, including routes that never call `provideToast()` (e.g. a cold navigation to `/dashboard/settings` or `/dashboard/health`, or an auth page). On those the bridge was dormant, so the notification was added to `UIStore` but never rendered — history-dependent behavior (it worked only after a toast-firing route had been visited). **Fix:** the interceptor now activates the bridge **on demand** — it injects `Injector` and calls `injector.get(ToastBridgeService)` inside its `403` branch, immediately before `showNotification` — so the single root bridge is instantiated exactly when a 403 occurs and the toast renders on any route. This is deliberately **not** an always-on root activation (which would keep the bridge live app-wide); the bridge stays dormant until a 403 actually fires. The SSR `isPlatformBrowser` guard is unchanged. **Product decision:** a 403 surfaces its toast on every page (including public/auth) by design — that was already the interceptor's intent; it just silently failed to render off toast-routes. Covered by a new `forbidden.interceptor.spec.ts` case (real bridge + real `UIStore` + mock `NgpToastManager`, asserting `show()` is called on a 403 and not on a non-403) and an e2e that cold-loads `/dashboard/health` with a mocked 403 and asserts the toast renders. **Migration:** none — no API, schema, or HTTP-contract change. ([#480](https://github.com/ResetShop/angular-nx-standalone-starter/issues/480))
- **Permission deny toast fired twice when deep-linking a gated detail route (`/dashboard/users/:id`).** Deep-linking a permission-gated parameterized route as a user without the permission surfaced the `PERMISSIONS.ERRORS.ACCESS_DENIED` toast **twice** (vs once on the param-less list route). Surfaced while writing the #462 e2e coverage. Root cause: `provideToast()` listed `NgpToastManager` and `ToastBridgeService` as **bare-class providers** in each toast-firing route (`users`, `users/:id`, `authorization/roles`). Both are `providedIn: 'root'` singletons, but a bare class in a route's `providers` is `useClass` — it overrides the root singleton and mints a **route-scoped** instance. Since every `ToastBridgeService` watches the **shared, global** `UIStore.notifications()`, two live route-scoped bridges (the denied `users/:id` injector plus another live one) both rendered the single deny notification — two `<p class="toast-message">` elements for one `UIStore` entry (confirmed by instrumenting the bridge: one guard run, one notification, **two** `show()` calls from **two** `ToastBridgeService` instances). **Fix:** `provideToast()` no longer re-provides the manager/bridge — it is now **activation-only** (`provideEnvironmentInitializer(() => inject(ToastBridgeService))`), which eagerly instantiates the single root bridge so its `effect()` is live for the route without creating a new instance. `NgpToastManager` and `ToastBridgeService` stay `providedIn: 'root'` singletons (the manager renders into `document.body`, so its location is irrelevant), and **no `NgpToastConfig` is registered anywhere** — `ToastBridgeService` passes its presentation options (`placement: 'bottom-center'`, `duration`, `dismissible`) per `show()`, so no toast config is forced onto routes that never show toasts; container-only settings (`maxToasts`, `gap`, `zIndex`) use ng-primitives' defaults (`maxToasts` is already 3). `provideToast()` stays on each toast-firing route (its correct, least-privilege home — the shell and non-toast routes carry no toast providers); every call now shares the one root bridge, so a notification renders exactly once regardless of how many toast routes are live. The #462 e2e detail-route deny test is tightened from `.first()` to a strict single-toast assertion as the regression guard. **Migration:** forks that copied the old `provideToast()` (which bundled `provideToastConfig` + `NgpToastManager` + `ToastBridgeService`) must drop the `provideToastConfig`/`NgpToastManager`/`ToastBridgeService` entries — `provideToast()` is now activation-only and toast presentation options are passed per `show()` in `ToastBridgeService`. No API, schema, or HTTP-contract change. (A related reliability gap for `forbiddenInterceptor`'s global 403 toast is tracked in [#480](https://github.com/ResetShop/angular-nx-standalone-starter/issues/480).) ([#471](https://github.com/ResetShop/angular-nx-standalone-starter/issues/471))
- **Unreachable "Go to dashboard" link on the public landing header.** The landing header (`apps/reference-app/src/app/components/landing-header/landing-header.ts`) rendered a `@if (isAuthenticated())`-guarded "Go to dashboard" link, but it was dead code: the public `/` route runs no session validation, nothing rehydrates `AuthStore.currentUser` on a cold load (no bootstrap `/api/auth/me`, no persistence), and there is no in-SPA navigation path to `/` from any authenticated area — so `isAuthenticated()` was always `false` on the only way to reach `/` and the link never showed. Surfaced while writing the #460 e2e coverage. **Per the product decision, the dead link is removed** (rather than adding a `/me` round-trip to every public page load, which the header's own comment deliberately avoided). The header is now a stateless component with no `AuthStore` dependency. Follow-on cleanup of the now-orphaned surface: the unused `LANDING.GO_TO_DASHBOARD` key is removed from `TranslationSchema` (`packages/angular-core/src/lib/i18n/translations.schema.ts`), `en.ts`, `es.ts`, and the app test mock (`translation.mock.ts`); the now-dead `provideAuthStateMock` test helper (`apps/reference-app/src/app/store/auth/auth.store.mock.ts`, its only consumers being the landing header/page specs + stories) is deleted; the landing header/page specs and stories drop their authenticated/unauthenticated variants; and the #460 e2e landing assertions are reframed from a "known gap #468" to design-intent regression guards (the header has no dashboard link in any auth state). **Migration:** forks that hand-maintain their own translation files must remove the `LANDING.GO_TO_DASHBOARD` key (an extra key now produces a TypeScript excess-property error on the `TranslationSchema`-typed `en` / `es` constants); forks importing `provideAuthStateMock` from `auth.store.mock.ts` must drop it (the helper is gone). No HTTP contract, API, or DB-schema change. ([#468](https://github.com/ResetShop/angular-nx-standalone-starter/issues/468))
- **Production build failure in the SSR route-extraction worker — `changePasswordRateLimiter` read `authEnv` at module-eval.** `nx build reference-app:production` failed during Angular route extraction with `FATAL: Environment validation failed (auth domain): PASETO_SECRET_KEY / PASETO_ISSUER … received undefined`. The rate limiter in `apps/reference-app/src/api/middlewares/rate-limit.middleware.ts` was a module-scope `const` built via `rateLimiter({ windowMs: parseDurationToMs(authEnv.AUTH_CHANGE_PASSWORD_RATE_LIMIT_WINDOW), … })` — touching any `authEnv` property parses the whole auth schema (requiring the PASETO vars). The SSR route-extraction / prerender worker imports the server bundle in an env-less context, so that eager read `process.exit(1)`'d the worker. Regression from the #354 stack ([#441](https://github.com/ResetShop/angular-nx-standalone-starter/issues/441)); it was masked in local runs where the PASETO vars happen to be exported in the shell (the worker inherits `process.env`). **Fix:** `changePasswordRateLimiter` is now built lazily on the first request via a `null`-sentinel wrapper (the same deferral pattern as the `cors()` middleware in `server.ts`), so importing the module reads no env. A `rate-limit.middleware.spec.ts` regression test imports the module with the PASETO vars unset and asserts it does not `process.exit`. Verified: `npm run build` now succeeds with `PASETO_SECRET_KEY`/`PASETO_ISSUER` unset. **Migration:** none — no API, schema, or HTTP-contract change. ([#450](https://github.com/ResetShop/angular-nx-standalone-starter/issues/450))
- **Stale password-reset confirmation persisting across navigation.** `AuthStore.resetRequested` (flipped optimistically when a reset is requested) lives in the root-scoped store and was never reset, so returning to `/auth/reset-password` after a prior request showed the lingering "if an account exists, a link was sent" confirmation instead of a fresh form; a stale `resetPasswordError` could likewise survive on the reset-confirm page. A new `AuthStore.clearResetState()` resets `resetRequested`, `isResettingPassword`, and `resetPasswordError` together, and both `reset-password.ts` and `reset-password-confirm.ts` call it **synchronously in their constructors** on entry — before the first render, so the fresh form/state shows with no flash of stale content. Concern B from the issue (a robust timer-based temporary login-lockout UX) is tracked separately as a follow-up. **Migration:** none — no API, schema, or HTTP-contract change. ([#434](https://github.com/ResetShop/angular-nx-standalone-starter/issues/434))
- **Forgot-password account-enumeration timing oracle (frontend + backend).** The self-service forgot-password flow returned a neutral `200` with an identical body, but its _latency_ still leaked whether an email belonged to an active account: the backend short-circuited for unknown emails yet did real work (invalidate prior tokens, generate + SHA-256-hash a token, `await` the email send) for existing ones, and the frontend only flipped `resetRequested` in the `rxMethod`'s `next`/`error` handlers — i.e. after the round-trip — surfacing that delta directly in the UI. **Frontend** (`apps/reference-app/src/app/store/auth/auth.store.ts`): `forgotPassword` now flips `resetRequested` optimistically in the leading `tap` (before the request is even sent) and fires the request best-effort (`switchMap` + `catchError(() => EMPTY)`); failures are still logged via `Logger.error` for observability but never surfaced or rolled back. The obsolete `isRequestingReset` state is removed from `AuthState`/`initialAuthState`, the `reset-password` page's submit-button `[disabled]` guard, and the Storybook mock. **Backend** (`apps/reference-app/src/api/modules/auth/auth.controller.ts`): the handler now runs `requestPasswordReset` via the new `deferAfterResponse()` helper and returns the neutral `200` immediately, so the unknown-email path (a single DB read) and the active-account path (DB writes + SMTP) respond identically. **New API** — `deferAfterResponse(c, work, { onError })` in `@resetshop/hono-core` (`packages/hono-core/src/lib/defer-after-response.ts`): runs post-response work safely on both a long-lived Node server (floating task on the event loop) and a serverless/edge runtime (registered via `executionCtx.waitUntil` so the platform does not freeze and discard it); deferred failures are routed to the required `onError` callback and never silently dropped. **Migration:** forks reading `AuthStore.isRequestingReset()` must drop it — the field is gone (the confirmation is now optimistic); no HTTP contract, schema, or response-body change — `POST /api/auth/forgot-password` still returns the same neutral `200`. ([#433](https://github.com/ResetShop/angular-nx-standalone-starter/issues/433))
- **Flaky `generatePassword` ASCII-words test.** The English diceware word list (`apps/reference-app/src/api/utils/wordlists/en-password-seed.txt`) contained four hyphenated/compound entries (`drop-down`, `felt-tip`, `t-shirt`, `yo-yo`) that intermittently failed the `password.spec.ts` `/^[a-z]+$/` assertion (~3% of runs) when `generatePassword()`'s RNG happened to draw one — non-deterministic CI reds unrelated to the change under test. The entries are de-hyphenated in place (`dropdown`, `felttip`, `tshirt`, and `yurt` for `yo-yo` to avoid duplicating the existing `yoyo`), preserving the 7,776-word count and per-word uniqueness. The two language-selection specs no longer sample generated passwords: they now assert invariants over the entire source list via the newly-exported `getWordList()` — the English list contains only `^[a-z]+$` words, and the Spanish path is validated deterministically (every generated word is a member of the `es` list, and the `es` list genuinely contains accented/non-ASCII words). **Migration:** none — generated temporary passwords remain random dot-separated lowercase passphrases; only four seed words changed. ([#429](https://github.com/ResetShop/angular-nx-standalone-starter/issues/429))
- **Sidebar mobile semantics, overlay width, and brand truncation** — four responsiveness defects addressed: (1) Collapse toggle button and Ctrl+B / Cmd+B shortcut now only activate at the `lg` breakpoint (≥1024px) via a `BreakpointObserver`-driven signal local to `Sidebar` that reads Tailwind 4's `--breakpoint-lg` CSS custom property at runtime (with a `64rem` fallback), preventing collapsed state from applying on mobile where the sidebar renders as an overlay sheet. (2) Mobile overlay width changed from fixed `280px` to `min(280px, 80vw)`, capping at 80% of the viewport on narrow phones (≤350px). (3) Brand component text wrapped in `<span class="min-w-0 truncate">` so it ellipsis-truncates instead of overflowing the sidebar. (4) Removed the fragile `whitespace-nowrap` from the sidebar `:host` — the previous purpose was preventing text wrapping, but it also prevented all internal wrapping; truncation on individual elements is the correct approach. Architecturally, the viewport-gated `isCollapsed` computed lives in `Sidebar` and propagates `collapsed` downward as a plain `input()` to `Brand`, `NavSection`, and `NavItem` — replacing the previous approach where each child injected `UIStore` directly. This decouples the children from store state and improves testability. Mobile sidebar story gains explicit `layout: 'fullscreen'` and viewport accuracy JSDoc note. Dashboard gains mobile backdrop overlay tests (render, click-to-close, removal). ([#340](https://github.com/ResetShop/angular-nx-standalone-starter/issues/340))
- **`packages/ui/eslint/form-field-allowed-content.spec.js` resurrected as executable.** Its `import` pointed at `./form-field-allowed-content.eslint-rule.js`, a file that has never existed in the repo — the actual rule file is `form-field-allowed-content.js`. Compounding the issue, `packages/ui/vitest.config.ts` only included `src/**/*.{test,spec}.ts`, so the `eslint/` directory was outside the test glob and the broken import would have been invisible regardless. Three changes restore the spec: (a) the import is rewritten to `./form-field-allowed-content.js`; (b) the vitest `include` glob is extended to `['src/**/*.{test,spec}.ts', 'eslint/**/*.spec.{js,ts}']`; (c) both ESLint specs bind Vitest's `describe`/`it`/`it.only` to ESLint's `RuleTester` static properties (`RuleTester` defaults to Mocha-style globals which Vitest does not provide). Net effect: the existing `form-field-allowed-content` rule now has 11 passing tests in CI, and the new `storybook-source-state` rule has 8 — both rules' AST traversal logic is now CI-verified rather than relying on empirical evidence from the linted corpus. ([#348](https://github.com/ResetShop/angular-nx-standalone-starter/issues/348))
- **`@resetshop/generators:page` inner-directory casing** — the generator now produces fully kebab-case paths for compound names (e.g. `<dir>/order-history/order-history-list/order-history-list.ts`) instead of mixing kebab-case for the outer directory with camelCase for the inner `<%= name %>-list/` placeholder (which previously produced `<dir>/order-history/orderHistory-list/orderHistory-list.ts`). Single-word names (`product`, `order`) are unaffected. One-line change: `page/index.ts` now passes `name: n.fileName` to `generateFiles` instead of `name: n.propertyName`, matching the convention already used by the other generators. **Forks that have run `nx g @resetshop/generators:page <camelCaseName>` will need to rename the inner `*-list/` directory and its files from camelCase to kebab-case after this update.** ([#331](https://github.com/ResetShop/angular-nx-standalone-starter/issues/331))
- **`@resetshop/generators:drizzle-schema` filename casing** — the generator now writes files at `<directory>/<kebab-case>.ts` (e.g. `order-line-item.ts`) instead of `<camelCase>.ts` (e.g. `orderLineItem.ts`). Brings drizzle-schema in line with the `store`, `api-provider`, and `backend-module` generators (which already use `n.fileName` as the templateVar `name`) and matches the kebab-case file naming used everywhere else in `apps/reference-app/src/db/schema/`. Inside the generated file, JS identifiers and the table-name string stay camelCase (Drizzle's standard convention) — only the path on disk changes. **Forks that have run `nx g @resetshop/generators:drizzle-schema <camelCaseName>` and committed the resulting file will need to rename it to kebab-case after this update; single-word names (`product`, `order`) are unaffected.** Template variable cleanup: `__name__.ts.template` now uses `<%= propertyName %>` for the JS identifier and table-name string instead of the previously-overloaded `<%= name %>`. ([#330](https://github.com/ResetShop/angular-nx-standalone-starter/issues/330))
- **`package.json` `drizzle:seed` / `sync:permissions` scripts** — added `--tsconfig apps/reference-app/tsconfig.json` so tsx resolves the app-scoped `@contracts/*` / `@schema/*` path aliases. Running `npm run drizzle:seed` from the workspace root was failing with `ERR_MODULE_NOT_FOUND: Cannot find package '@contracts/user'` because tsx was resolving against `tsconfig.base.json`, where the app-scoped aliases are not declared.
- **`.github/workflows/ci.yml`** — fixed the broken bare-target form `npx nx test --skip-nx-cache` / `npx nx lint --skip-nx-cache` that was failing in modern Nx with `Cannot find configuration for task @app/source:test/lint`. Modern Nx looks for the target on the workspace root project (`@app/source`), which has no test/lint target — the workspace-wide invocation requires `npx nx run-many -t <target>`. Replaced both with `run-many` form. Also added a dedicated `typecheck` job (previously only run locally) and aligned the `e2e`/`build`/`storybook` jobs to the explicit `npx nx run reference-app:<target>:<config>` form for consistency. ([#319](https://github.com/ResetShop/angular-nx-standalone-starter/issues/319))
- **`package.json` `test:watch` / `test:ui` scripts** — converted from `nx test reference-app …` to the `nx run reference-app:test …` form used by the rest of the file (`storybook`, `storybook:build`, `test:e2e`). Behavior unchanged; aligns invocation style across all scripts. ([#319](https://github.com/ResetShop/angular-nx-standalone-starter/issues/319))
- **`.github/workflows/ci.yml`** — removed redundant `restore-keys: repo-${{ github.sha }}` from every cache step. The fallback was identical to the primary `key`, so a miss on the primary always missed the fallback too — the field added no coverage. Behavior unchanged. ([#319](https://github.com/ResetShop/angular-nx-standalone-starter/issues/319))
- **`apps/reference-app/playwright.config.ts`** — `webServer.command` was `nx run app:serve --port=3000`, referencing the legacy `app` project name that was removed in Epic 1 when `apps/reference-app` was created. Updated to `nx run reference-app:serve --port=3000`. Locally Playwright reuses an already-running dev server (`reuseExistingServer: !process.env.CI`) so the typo was hidden; on CI Playwright tries to spawn the server itself and Nx exits 1 because `app` doesn't exist. The `1.0.0-beta.3` sweep that fixed four other stale `nx run app:*` references in `ci.yml` missed this one. ([#319](https://github.com/ResetShop/angular-nx-standalone-starter/issues/319))
- **`.github/workflows/ci.yml`** — quoted `node-version: '22.20'` in all 9 occurrences (1 setup + 8 worker jobs). Unquoted, YAML parses `22.20` as the float `22.2`, so `actions/setup-node` was acquiring Node **22.2.0** on every CI run instead of 22.20.0. Node 22.2 predates the default-on `--experimental-require-module` (Node 22.12+), which is why `@angular/compiler-cli` and `@storybook/angular` were failing to load with `ERR_REQUIRE_ESM` in the `build` and `storybook` jobs. Quoting makes the version a string and forces the intended 22.20.x acquisition. ([#319](https://github.com/ResetShop/angular-nx-standalone-starter/issues/319))
- **`README.md` Project Setup Guide** — corrected 8 stale file references that still pointed at the pre-restructure single-app layout (`src/api/helpers/`, `src/app/`). Connector templates and environment config now correctly point at `packages/hono-core/src/lib/`; remaining app-specific paths now carry the `apps/reference-app/` prefix. A fork user following the optional integration instructions was previously hitting paths that no longer existed.
- **`packages/angular-core/src/lib/navigation/navigation-title.strategy.ts`** — translates `route.title` through `Translation.instant()` before writing to the document title. Previously the strategy passed the raw `route.title` value (an i18n key like `USERS.PAGE.TITLE`) straight to `Title.setTitle()`, so the browser tab read `USERS.PAGE.TITLE | <applicationName>` instead of `Users | <applicationName>`. Also added an `effect` that watches `Translation.currentLanguage` and re-applies the title when the active language changes between navigations (without it the tab title stayed frozen in whichever language was active at the last route change).
- **`apps/reference-app/src/contracts/auth/auth.schemas.ts`** — `meResponseSchema.email` was `z.string()` while `authUserSchema.email` was `z.email()`. Collapsing `meResponseSchema` into `authUserSchema` (see Changed entry above) tightens email validation on `/api/auth/me` to match `/api/auth/login`. ([#322](https://github.com/ResetShop/angular-nx-standalone-starter/issues/322))
- **Sidebar nav flicker during logout** — `provideAuth()` (`apps/reference-app/src/app/providers/auth/auth.provider.ts`) now accepts opt-in features in the `provideRouter`/`provideHttpClient` style. The new `withNavigationPermissionCheck()` feature wires the navigation library's `NAVIGATION_PERMISSION_CHECK` token through `AuthStore.currentUser`, and **returns `true` for any permission when no user is logged in**. The previous inline factory in `app.config.ts` re-evaluated `Navigation.sections` to a permission-stripped list the instant `authStore.logout()` cleared `currentUser`, producing a one-frame visible flicker before the route deactivated and the sidebar unmounted. Skipping the filter when no user is logged in eliminates the recomputation; route-level access is still enforced by `permissionGuard`, so this carries no security impact. **Migration for forks:** if your `app.config.ts` calls `provideAuth()` and you want the navigation permission check, change it to `provideAuth(withNavigationPermissionCheck())` and remove any inline `NAVIGATION_PERMISSION_CHECK` provider you may have copied. `provideAuth()` with no features is still valid and registers only the `AuthApi` token.

## 1.0.0-beta.3 — 2026-04-07

Final Epic 2 cleanup. Closes the milestone (`Monorepo restructure + fork distribution`).

### Changed

- **`.github/workflows/ci.yml`** — replaced four stale `nx run app:*` references (`stylelint`, `e2e`, `build:production`, `build-storybook`) with the current `nx run reference-app:*` invocations. The legacy `app` project name was removed when `apps/reference-app` was created in Epic 1; the CI workflow had not been updated alongside.
- **`.github/workflows/ci.yml`** — aligned all worker jobs to `node-version: 22.20` to match the `setup` job that builds the `node_modules` cache. Prior version mismatch (`22.12` on workers vs `22.20` on setup) was ABI-compatible but misleading.
- **`.github/workflows/upstream-guards.yml`** — removed redundant `&& github.event.pull_request != null` null checks from both job `if:` conditions. The `pull_request` event guarantees the field is non-null; the `github.repository ==` check alone is the meaningful gate.
- **`docs/forking.md`** — top-of-file status banner flipped from "target state with planned callouts" to "live as it exists today" now that Epic 2 is complete.

### Fixed

- **`apps/reference-app/project.json`** `serve-static` target — `staticFilePath` was `"dist/app/browser"` (the old single-app output dir). Build's `outputPath` is `"dist/reference-app"`, so the correct serve path is `"dist/reference-app/browser"`. The target had been silently serving from a non-existent directory.
- **`apps/reference-app/src/api/utils/password.ts`** comment — said `"copied to dist/app/server/wordlists/"`. Updated to `dist/reference-app/server/wordlists/`. Runtime was unaffected (the code uses `import.meta.dirname`), but the comment misled developers debugging wordlist loading.

## 1.0.0-beta.2 — 2026-04-07

Adds the upstream CI guard layer that protects the starter contract on incoming PRs.

### Added

- **`.github/workflows/upstream-guards.yml`** — single workflow with two jobs:
  - **`boundary-guard`** — fails if a PR touches any path under `apps/` other than `apps/reference-app` or its subdirectories. Sibling-named directories (e.g. `apps/reference-app-staging/`) are deliberately treated as offending. Bypass label: `allow-app-change`.
  - **`changelog-guard`** — fails if a PR modifies starter-owned code (anything under `packages/`, `apps/reference-app/`, `scripts/`, `docs/`, `drizzle/`, `e2e/`, `.github/`, `.claude/`, plus the root config files) without adding an entry to `CHANGELOG.md`. Bypass label: `skip-changelog`. `CHANGELOG.md` itself is intentionally excluded from the trigger to avoid a circular requirement.
  - Both jobs are gated to `github.repository == 'ResetShop/angular-nx-standalone-starter'` so they never fire on forks. Both re-evaluate when bypass labels are added or removed via `pull_request: types: [opened, synchronize, reopened, labeled, unlabeled]`.
- **`.github/pull_request_template.md`** — Fork-distribution checklist (CHANGELOG entry, no fork-owned paths touched, CI guards reviewed). Top comment notes the template applies to PRs against the upstream repo only; forks may delete or replace it.

### Changed

- **`docs/forking.md`** — §9 (CI guards) converted from planned to active tense; §5 (conflict resolution) forward reference to PR 2.4 resolved.

## 1.0.0-beta.1 — 2026-04-07

Adds the starter/app boundary enforcement layer on top of the structural restructure shipped in `1.0.0-beta.0`.

### Added

- **Nx tag scheme `scope:starter` / `scope:app`** — all upstream-owned projects (`packages/*` and `apps/reference-app`) carry `scope:starter`; fork-generated apps emit `scope:app` from the schematic. The schematic's tag rewrite now actually fires against the renamed workspace.
- **ESLint `@nx/enforce-module-boundaries` `depConstraints` for `scope:*`** — `scope:starter` projects may only depend on other `scope:starter` projects; `scope:app` projects may depend on both `scope:starter` and `scope:app`. Stacks with the existing `type:*` constraints.
- **ESLint cross-boundary relative-import bans** — two new flat-config blocks (`no-cross-boundary-relative-imports-from-packages` and `no-cross-boundary-relative-imports-from-apps`) using depth-agnostic `^(\.\./)+(apps|packages)/` regex patterns. Forces consumers to use the `@<scope>/*` package aliases instead of relative paths across the boundary.

### Changed

- **`nx.json` `defaultBase` changed from `master` to `main`** — matches the actual upstream branch name; fixes silent breakage of `nx affected` and Nx Cloud CIPE diffs. **Fork action:** if your fork's primary branch is not `main`, change `defaultBase` once to match your branch name after merging this version.

### Removed

- **`nx.json` `defaultProject`** — relied on by direct `nx` CLI calls which are forbidden by CLAUDE.md's command policy. Removing it eliminates a fork-merge conflict surface and forces explicit project naming in every command.

## 1.0.0-beta.0 — 2026-04-06

This is the first tagged version of the starter under the fork-distribution model. Forks created after this version should track the changelog entries above as the canonical record of what has changed since their initial fork point.

### Added

- **Fork-distribution model.** The repository is now designed to be forked rather than consumed as a published package set. Starter code lives in `packages/*` and `apps/reference-app`; fork apps live in `apps/<your-app>`.
- **`@resetshop/generators:app` schematic** — clones `apps/reference-app` into `apps/<slug>` for new app creation. Invoked via `npm run generate:app -- --name="My App"`. Includes 20 unit tests and a smoke-test pass against the live workspace.
- **`docs/forking.md`** — full forking workflow documentation: ownership boundaries, initial setup, app creation, upstream merge process with conflict resolution conventions, the changelog contract, and what NOT to do.
- **README forking section** — quickstart snippet pointing at `docs/forking.md`.
- **`packages/hono-core` exports `isServerless()`** — moved from a per-app helper file in `apps/reference-app/src/api/utils/environment.ts` so backend modules across multiple apps can share the same runtime check.
- **Uniform CI target set** — every project under `packages/*` and `apps/*` now exposes the targets it legitimately needs (`build`, `test`, `typecheck`, `lint`, `stylelint`, `build-storybook` where applicable).
- **`CLAUDE.md` Canonical App Creation Workflow section** — explicit policy that new apps must always be created via the schematic; direct hand-copying of `apps/reference-app` is forbidden.
- **Coding agent collaboration policy** — `.claude/references/coding-agent-policies.md` codifies the rule against shortcut framings on solo-maintainer assumptions, plus other anti-patterns. Referenced from `CLAUDE.md` as a hard constraint.

### Changed

- **Monorepo restructured** under `packages/*` and `apps/reference-app/src/`. UI components live in `@resetshop/ui`, Angular providers in `@resetshop/angular-core`, Hono backend infrastructure in `@resetshop/hono-core`, framework-free utilities in `@resetshop/util`, and Nx generators in `@resetshop/generators`. All app-specific code (stores, providers, API modules, drizzle schemas, contracts) lives under `apps/reference-app/src/`.
- **Tailwind config moved to workspace root.** `tailwind.config.css` now lives at the workspace root and globs into both `apps/*` and `packages/*` via `@source` directives, so all projects pick up the same theme.
- **Backend imports migrated from relative `'../../openapi-app'` to `@resetshop/hono-core`** across the reference app's API controllers and repositories. Forks consuming the reference template see no behavioural change but should use the package alias for any new backend code.
- **`package.json` `dependencies` and `devDependencies` strictly alphabetized** — minimizes conflict surface for forks adding their own dependencies.

### Removed

- **Single-app starter layout.** The original root-level `src/`, `project.json`, `tsconfig.app.json`, `tsconfig.spec.json`, `playwright.config.ts`, `vitest.config.ts`, `vitest.integration.config.ts`, and `tools/` directories have been removed. Their content lives under `apps/reference-app/` or `packages/*/eslint/` now. **Fork action:** any fork-local imports via the old root-relative paths must be updated to use `apps/reference-app/...` paths or `@resetshop/*` aliases.
- **`apps/reference-app/src/api/openapi-app.ts`** and **`apps/reference-app/src/api/utils/environment.ts`** — replaced by `@resetshop/hono-core` exports.

### Notes — deferred work tracked in GitHub Issues

- **`hono-core` environment.ts split** — `isServerless()` currently sits next to an app-specific `environment` constant with a pre-existing type unsoundness. To be split. ([#258](https://github.com/ResetShop/angular-nx-standalone-starter/issues/258))
- **Hardcoded millisecond constants sweep** — codebase-wide migration to the duration-string utilities. ([#259](https://github.com/ResetShop/angular-nx-standalone-starter/issues/259))
- **Pluggable starter scope options** — the `app` schematic currently only supports a full clone of `reference-app`. A future iteration will add `--starter` presets. ([#260](https://github.com/ResetShop/angular-nx-standalone-starter/issues/260))
- **`apps/reference-app` e2e test coverage** — Playwright config exists but no specs. ([#261](https://github.com/ResetShop/angular-nx-standalone-starter/issues/261))

<!--
  Link references. The v1.0.0 tag is created on main after the #485 PR merges,
  at which point the [1.0.0] link below resolves to its GitHub release.
  The beta releases (1.0.0-beta.0–beta.3) were never tagged upstream, so their
  section headings above are plain text and have no link references here.
-->

[Unreleased]: https://github.com/ResetShop/angular-nx-standalone-starter/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/ResetShop/angular-nx-standalone-starter/releases/tag/v1.0.0
